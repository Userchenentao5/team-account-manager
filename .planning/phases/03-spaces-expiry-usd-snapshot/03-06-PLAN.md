---
phase: 03-spaces-expiry-usd-snapshot
plan: 06
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [SPACE-01, SPACE-02, SPACE-03, SPACE-04, ACCT-01, EXP-01, FX-02]
must_haves:
  truths:
    - "Phase 03 remains Mode: mvp and its ROADMAP goal is strict user-story format: `As a ..., I want ..., so that ... .`"
    - "The revised Phase 03 goal preserves the existing technical meaning: full space CRUD, one mother account, expiry dates, and frozen USD snapshots computed at payment time."
    - "The revised goal does not add source-code scope, new requirements, deferred Phase 4 child-account CRUD, deferred Phase 4 space deletion, or Phase 5 dashboard work."
    - "`user-story.validate --pick valid` returns `true` for the revised Phase 03 goal."
    - "Phase 03 verification is rerun after the metadata fix and no longer reports the MVP user-story guard gap."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 03 goal in strict MVP user-story format"
    - path: ".planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md"
      provides: "Updated Phase 03 verification result after the metadata fix"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "gsd user-story validator"
      via: "Phase 03 Goal text is passed to `user-story.validate --pick valid`"
      pattern: "As a .+, I want to .+, so that .+\\."
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md"
      via: "Phase 03 verification reads the updated MVP goal and clears the existing guard failure"
      pattern: "status: passed"
---

<objective>
Close the single Phase 03 verification gap by correcting process metadata only: rewrite the Phase 03 ROADMAP goal into strict MVP user-story format while preserving its existing technical meaning, validate the story, and rerun Phase 03 verification.

Purpose: Phase 03 implementation, tests, build, lint, and UAT are already verified; canonical MVP verification is blocked only because `Mode: mvp` requires a strict `As a ..., I want ..., so that ... .` goal.
Output: Updated `.planning/ROADMAP.md`, refreshed `.planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md`, and `.planning/phases/03-spaces-expiry-usd-snapshot/03-06-SUMMARY.md`.
</objective>

<execution_context>
@C:/Users/30952/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/30952/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-spaces-expiry-usd-snapshot/03-CONTEXT.md
@.planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md
@.planning/phases/03-spaces-expiry-usd-snapshot/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Phase 03 goal to strict MVP user story</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 03 section only; preserve Phase 03 Mode, Depends on, Requirements, Success Criteria, Plans, and all other phase sections)
    - .planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md (frontmatter gap and "MVP User Story Guard" section)
    - .planning/phases/03-spaces-expiry-usd-snapshot/03-CONTEXT.md (Phase 03 boundary, locked decisions D-01 through D-09, and deferred items)
    - .planning/REQUIREMENTS.md (confirm Phase 03 requirement IDs remain SPACE-01, SPACE-02, SPACE-03, SPACE-04, ACCT-01, EXP-01, FX-02)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>Update only the `**Goal**` line in the Phase 03 ROADMAP section. Replace the existing delivery-goal sentence with this strict MVP user story unless a better wording is needed to preserve the same meaning: `As a team subscription manager, I want to fully manage subscription spaces and each space's mother account with expiry dates and frozen USD cost snapshots computed at payment time, so that I can see which spaces need renewal and understand their normalized USD cost without recomputing historical rates.` Keep `**Mode:** mvp`, `**Depends on**`, `**Requirements**`, `**Success Criteria**`, existing plan list, and all completed status markers unchanged. Do not add Phase 4 deferred child-account CRUD or cascade-delete scope, and do not add Phase 5 dashboard scope. This task implements the verification gap, not a product change.</action>
  <verify>
    <automated>git diff -- .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 03 `**Goal**` line starts with `As a `, contains `, I want to `, contains `, so that `, and ends with a period.
    - The revised goal still mentions subscription spaces, the single mother account, expiry dates, frozen USD cost snapshots, and payment-time computation.
    - The diff for `.planning/ROADMAP.md` is limited to the Phase 03 goal line.
    - No source code, shipped Phase 03 plans, summaries, UAT, or other roadmap phases are modified.
  </acceptance_criteria>
  <done>Phase 03 has a strict MVP user-story goal in `.planning/ROADMAP.md` with unchanged implementation scope.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the revised user story</name>
  <read_first>
    - .planning/ROADMAP.md (the exact revised Phase 03 `**Goal**` line)
    - .planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md (the previous failed `user-story.validate` evidence)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>Run the GSD user-story validator against the exact revised Phase 03 goal text from `.planning/ROADMAP.md`. Use the local GSD tool path already used by this project. Record the successful command and result in the eventual `03-06-SUMMARY.md`; do not edit source code. The expected validator output for the suggested story is valid with role `team subscription manager`, capability covering full space/mother-account management with expiry and frozen USD snapshots, and outcome covering renewal visibility plus normalized USD cost without recomputing historical rates.</action>
  <verify>
    <automated>node C:/Users/30952/.codex/gsd-core/bin/gsd-tools.cjs query user-story.validate --story "As a team subscription manager, I want to fully manage subscription spaces and each space's mother account with expiry dates and frozen USD cost snapshots computed at payment time, so that I can see which spaces need renewal and understand their normalized USD cost without recomputing historical rates." --pick valid</automated>
  </verify>
  <acceptance_criteria>
    - `user-story.validate --pick valid` returns exactly `true` for the revised Phase 03 goal.
    - If the final wording differs from the suggested story, the command is rerun with the final exact ROADMAP text and returns `true`.
    - The validation evidence is recorded in `03-06-SUMMARY.md`.
  </acceptance_criteria>
  <done>The revised Phase 03 goal passes the GSD strict user-story validator.</done>
</task>

<task type="auto">
  <name>Task 3: Rerun Phase 03 verification after metadata fix</name>
  <read_first>
    - .planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md (existing `status: gaps_found`, score 14/15, and single MVP metadata gap)
    - .planning/phases/03-spaces-expiry-usd-snapshot/03-UAT.md (already-passed human UAT evidence, if the verifier asks for UAT context)
    - .planning/phases/03-spaces-expiry-usd-snapshot/03-01-SUMMARY.md through 03-05-SUMMARY.md (existing implementation evidence only if the verifier needs it)
  </read_first>
  <files>.planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md</files>
  <action>Rerun Phase 03 verification after the ROADMAP metadata fix. The correct routing command is the project GSD verification workflow for Phase 03, e.g. `$gsd-verify-work 03` or the equivalent local GSD verifier invocation available in this runtime. Verification should reuse the already-passed implementation, automated test, build, lint, and UAT evidence; do not change source code or replan technical work. If the verifier rewrites `.planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md`, ensure the only intended gap is closed and that any remaining issue is genuine new verifier output, not the old user-story guard.</action>
  <verify>
    <automated>node C:/Users/30952/.codex/gsd-core/bin/gsd-tools.cjs query verification.status .planning/phases/03-spaces-expiry-usd-snapshot --pick status</automated>
  </verify>
  <acceptance_criteria>
    - Phase 03 verification is rerun after Task 1 and Task 2.
    - The refreshed `03-VERIFICATION.md` no longer contains the failed MVP user-story guard as an open gap.
    - `verification.status` for `.planning/phases/03-spaces-expiry-usd-snapshot` returns `passed`, or the executor records the exact new verifier output if an unrelated new gap appears.
    - No source files are changed during this metadata-only closure.
  </acceptance_criteria>
  <done>Phase 03 canonical verification reflects the metadata fix and the prior technical verification remains intact.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- `.planning/ROADMAP.md` - Phase 03 goal converted to strict MVP user-story format.
- `.planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md` - refreshed Phase 03 verification result after the metadata fix.
- `.planning/phases/03-spaces-expiry-usd-snapshot/03-06-SUMMARY.md` - executor summary with the validator command output and final verification status.
</artifacts_produced>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | Phase 03 metadata gap | Phase 03 is `Mode: mvp` but current ROADMAP goal is not strict user-story format | 03-06 | COVERED | Task 1 rewrites only the goal line; Task 2 validates it; Task 3 reruns verification |
| REQ | SPACE-01, SPACE-02, SPACE-03, SPACE-04, ACCT-01, EXP-01, FX-02 | Existing technical Phase 03 requirements | 03-01..03-05 | COVERED | Already verified in `03-VERIFICATION.md`; this plan does not alter scope |
| RESEARCH | No new research item | No external dependency or technical implementation change | 03-06 | COVERED | Gap is process metadata only |
| CONTEXT | D-01 through D-09 | Locked Phase 03 implementation decisions | 03-01..03-05, 03-06 | COVERED | This plan preserves D-01 through D-09 and changes only the goal wording |
| CONTEXT | Deferred items | Child-account CRUD, cascade delete, dashboard | none | EXCLUDED | Explicitly scoped to Phase 4/Phase 5; not planned here |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning metadata -> GSD verifier | ROADMAP text is machine-read by the MVP user-story validator and verification status router |
| Local executor -> repository files | Executor updates `.planning/ROADMAP.md` and may refresh `03-VERIFICATION.md` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Tampering | `.planning/ROADMAP.md` Phase 03 section | medium | mitigate | Task 1 limits edits to the Phase 03 `**Goal**` line and requires `git diff -- .planning/ROADMAP.md` review before verification. |
| T-03-18 | Repudiation | validation evidence | low | mitigate | Task 2 records the exact `user-story.validate` command/result in `03-06-SUMMARY.md`. |
| T-03-19 | Tampering | shipped Phase 03 implementation and plan files | medium | mitigate | Plan files and source code are out of scope; files_modified is limited to ROADMAP plus verification artifact. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install tasks exist in this gap-closure plan. |
</threat_model>

<verification>
- `node C:/Users/30952/.codex/gsd-core/bin/gsd-tools.cjs query user-story.validate --story "<final Phase 03 goal>" --pick valid` returns `true`.
- Phase 03 verification is rerun and `node C:/Users/30952/.codex/gsd-core/bin/gsd-tools.cjs query verification.status .planning/phases/03-spaces-expiry-usd-snapshot --pick status` returns `passed`.
- `git diff --name-only` for this execution contains only `.planning/ROADMAP.md`, `.planning/phases/03-spaces-expiry-usd-snapshot/03-VERIFICATION.md`, and the generated `.planning/phases/03-spaces-expiry-usd-snapshot/03-06-SUMMARY.md`.
</verification>

<success_criteria>
The Phase 03 MVP metadata gap is closed: the ROADMAP goal is a strict user story, GSD user-story validation passes, Phase 03 verification is rerun, and the prior technical implementation scope remains unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-spaces-expiry-usd-snapshot/03-06-SUMMARY.md` when done.
</output>
