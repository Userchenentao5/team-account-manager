---
phase: 03-spaces-expiry-usd-snapshot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/
autonomous: true
requirements: [ACCT-01]
must_haves:
  truths:
    - "A `mother_account` table exists in the live SQLite schema, 1:1 with `space` (one mother per space, enforced by the DB)."
    - "A second mother_account row for the same space_id is rejected by the database, not by app code."
    - "Deleting a space cascades to its mother_account row (FK reserved for SPACE-05, Phase 4)."
  artifacts:
    - "src/db/schema.ts — exported `motherAccount` sqliteTable + `MotherAccountRow` type"
    - "drizzle/000X_*.sql — committed generated migration that creates `mother_account`"
  key_links:
    - "mother_account.space_id → space.id : NOT NULL + UNIQUE (1:1) + onDelete cascade"
    - "Test harness (src/test/db-harness.ts) applies ./drizzle; the committed .sql is the only path that makes the table exist for downstream tests"
---

<objective>
Add the `mother_account` table (D-04/D-05, ACCT-01) as a standalone table 1:1 with `space`, then generate and run the Drizzle migration so the table exists in the live DB and in the `./drizzle` set that the test harness applies.

Purpose: ACCT-01 models each space's single mother account as its own table (not a `space` column) to pave the way for Phase 4 child accounts and SPACE-05 cascade delete. The 1:1 and cascade are DB-enforced, race-free.
Output: `motherAccount` schema export + a committed `drizzle/000X_*.sql` migration creating `mother_account`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-spaces-expiry-usd-snapshot/03-CONTEXT.md
@.planning/phases/03-spaces-expiry-usd-snapshot/03-PATTERNS.md
@src/db/schema.ts
@src/test/db-harness.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the motherAccount 1:1 table to schema.ts</name>
  <read_first>
    - src/db/schema.ts (the file being modified — mirror the existing `paymentChannel` sqliteTable style at lines 23-30 and the `space` FK style at lines 60-65; place the new table AFTER the `space` table at line 78)
    - .planning/phases/03-spaces-expiry-usd-snapshot/03-PATTERNS.md (Pattern 1: `mother_account` 1:1 table, lines 38-62)
  </read_first>
  <files>src/db/schema.ts</files>
  <action>
    Append a new `motherAccount` export to src/db/schema.ts using `sqliteTable("mother_account", {...})` with exactly three columns: `id` = `integer("id").primaryKey({ autoIncrement: true })`; `spaceId` = `integer("space_id").notNull().unique().references(() => space.id, { onDelete: "cascade" })`; `email` = `text("email").notNull()`. The `.unique()` on `space_id` is what enforces the 1:1 relationship (D-04); `onDelete: "cascade"` reserves SPACE-05 cascade delete for Phase 4. Store ONLY email/login name — NO password or credential column (D-05, project constraint). Export the row type as `export type MotherAccountRow = typeof motherAccount.$inferSelect;` following the schema.ts type-export convention at line 49. Do NOT add or alter any `space` column — the `space` table already declares all FX/period/expiry columns nullable (lines 56-78) and needs no column migration this phase (research A2).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'export const motherAccount' src/db/schema.ts` returns a match.
    - The motherAccount block contains `.unique()` AND `onDelete: "cascade"` on the `space_id` reference.
    - `grep -n 'export type MotherAccountRow' src/db/schema.ts` returns a match.
    - No `password`, `secret`, or credential-like column appears in the motherAccount table (D-05).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>`motherAccount` table + `MotherAccountRow` type exist in schema.ts; types compile; the `space` table is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate + run the mother_account migration</name>
  <read_first>
    - drizzle/ (existing migration files: 0000_sudden_tony_stark.sql, 0001_charming_rawhide_kid.sql, meta/ — the new file follows this numbering)
    - package.json (scripts: `db:generate` = drizzle-kit generate, `db:migrate` = tsx src/db/migrate.ts)
  </read_first>
  <files>drizzle/</files>
  <action>
    [BLOCKING] This task is mandatory and must complete AFTER Task 1 (schema change) but BEFORE any downstream verification — build/type checks pass without it (types come from schema config, not the live DB), so skipping it creates a false-positive. Run `pnpm db:generate` (drizzle-kit generate) to produce a new `drizzle/000X_*.sql` containing `CREATE TABLE ... mother_account` plus the unique index on `space_id` and the FK with `ON DELETE cascade`. Then run `pnpm db:migrate` (tsx src/db/migrate.ts) to apply it to the local DB. Commit the generated `.sql` and the updated `drizzle/meta` snapshot — the committed migration is the evidence and is what `createTestDb()` applies via `./drizzle` for all downstream DB/action tests (db-harness.ts line 19). drizzle-kit generate/migrate is non-interactive here, so this stays autonomous.
  </action>
  <verify>
    <automated>grep -rl "mother_account" drizzle/ | grep -E '\.sql$' && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - A new `drizzle/000X_*.sql` exists and `grep -i 'create table' <new sql>` shows the `mother_account` table.
    - The new `.sql` references `space_id`, a UNIQUE constraint/index on it, and `on delete cascade`.
    - `drizzle/meta/_journal.json` includes the new migration entry.
    - `pnpm test` runs green (the harness applies the new migration without error; existing Phase 1-2 tests still pass).
  </acceptance_criteria>
  <done>New migration generated, applied, and committed; full existing test suite passes against the migrated schema.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- **Table:** `mother_account` (SQLite) — columns: `id` (PK, autoincrement), `space_id` (int, NOT NULL, UNIQUE, FK → `space.id` ON DELETE CASCADE), `email` (text, NOT NULL).
- **Symbol:** `motherAccount` (Drizzle table) exported from `src/db/schema.ts`.
- **Type:** `MotherAccountRow = typeof motherAccount.$inferSelect` exported from `src/db/schema.ts`.
- **File:** `drizzle/000X_*.sql` (generated migration creating `mother_account`) + updated `drizzle/meta` snapshot.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → SQLite | Schema/migration defines the integrity rules (1:1, cascade) the app relies on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / Integrity | mother_account ↔ space | high | mitigate | DB-enforced 1:1 via `.unique()` on `space_id` + NOT NULL FK; app never relies on "check-then-insert". Verified by Plan 03's unique-rejection test. |
| T-03-02 | Information disclosure | mother_account columns | medium | mitigate | No password/credential column exists (D-05) — design-level mitigation; only email/login stored. |
| T-03-03 | Repudiation / orphan rows | space deletion (Phase 4) | low | accept | `onDelete: "cascade"` reserved now; actual delete path ships in SPACE-05 (Phase 4), out of scope here. |
</threat_model>

<verification>
- `motherAccount` table + `MotherAccountRow` exported from schema.ts; `npx tsc --noEmit` passes.
- New `drizzle/*.sql` migration creates `mother_account` with UNIQUE `space_id` + cascade FK; committed.
- `pnpm test` green against the migrated schema.
</verification>

<success_criteria>
The `mother_account` table exists in the live and test-harness schema, 1:1 with `space` and DB-enforced, with the cascade FK reserved for SPACE-05.
</success_criteria>

<output>
Create `.planning/phases/03-spaces-expiry-usd-snapshot/03-01-SUMMARY.md` when done.
</output>
