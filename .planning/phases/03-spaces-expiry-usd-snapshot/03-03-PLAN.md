---
phase: 03-spaces-expiry-usd-snapshot
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/validation/space.ts
  - src/db/spaces.ts
  - src/db/currencies.ts
  - src/db/fxRates.ts
  - src/db/spaces.query.test.ts
autonomous: true
requirements: [SPACE-02, SPACE-03, ACCT-01]
must_haves:
  truths:
    - "A space + its mother account are inserted in a single synchronous transaction (all-or-nothing); a second mother row for the same space is rejected by the DB."
    - "The space list is returned sorted by expiry date ascending by default, and can be filtered by country and/or payment channel (D-06)."
    - "A space detail query returns the space joined with its mother account, payment channel, and currency in one row shape (SPACE-03)."
    - "The cached rate for a currency and the currency's minor-unit exponent are readable by parameterized helpers for the freeze pipeline."
  artifacts:
    - "src/lib/validation/space.ts — `spaceFormSchema`, `SpaceFormInput`, `spaceIdSchema`"
    - "src/db/spaces.ts — `insertSpaceWithMother`, `listSpaces`, `getSpaceDetail`, `updateSpaceRow`, `SpaceRow`"
    - "src/db/currencies.ts — `getCurrencyMinorUnit`"
    - "src/db/fxRates.ts — `getRate` (added)"
    - "src/db/spaces.query.test.ts — SPACE-02 sort/filter + ACCT-01 1:1/cascade coverage"
  key_links:
    - "insertSpaceWithMother uses db.transaction((tx) => {...}) SYNCHRONOUS — no await inside"
    - "listSpaces orderBy(asc(space.expiryDate)) — YYYY-MM-DD text sorts chronologically; conditional where via and(...)"
    - "all helpers take an explicit `db` param — never import the @/db singleton, so they run against createTestDb()"
---

<objective>
Build the data-access and validation layer for spaces: the shared Zod schema (`validation/space.ts`), the parameterized DB helpers (`db/spaces.ts` for the atomic 2-table write, list sort/filter, and detail join; `db/currencies.ts` for the minor-unit lookup; `getRate` added to `db/fxRates.ts`), and the DB-helper test covering SPACE-02 and ACCT-01.

Purpose: Provide the float-free, parameterized, harness-runnable primitives the Server Action (Plan 04) and RSC pages (Plan 05) compose. The 1:1 mother constraint and sort/filter contract are proven here at the DB layer.
Output: validation schema + four data helpers + a passing DB-helper test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-spaces-expiry-usd-snapshot/03-RESEARCH.md
@.planning/phases/03-spaces-expiry-usd-snapshot/03-PATTERNS.md
@src/db/schema.ts
@src/db/channels.ts
@src/db/fxRates.ts
@src/lib/validation/channel.ts
@src/test/db-harness.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Space validation schema (SPACE-01 inputs, mass-assignment guard)</name>
  <read_first>
    - src/lib/validation/channel.ts (analog — shared client+server schema, doc-comment trust-boundary note, z.infer type export, separate *IdSchema)
    - .planning/phases/03-spaces-expiry-usd-snapshot/03-PATTERNS.md (validation/space.ts assignment lines 192-212)
  </read_first>
  <files>src/lib/validation/space.ts</files>
  <behavior>
    - spaceFormSchema parses ONLY: name, country, paymentChannelId, currencyCode, amountMinor, openingDate, periodUnit, periodCount, motherEmail — extra keys are stripped (mass-assignment guard).
    - periodUnit accepts only "month"|"quarter"|"year" (D-09); openingDate matches /^\d{4}-\d{2}-\d{2}$/; amountMinor/paymentChannelId/periodCount are positive ints; country length 2; currencyCode length 3; name + motherEmail trimmed non-empty.
  </behavior>
  <action>Create src/lib/validation/space.ts mirroring validation/channel.ts. Export `spaceFormSchema = z.object({...})` with: `name` z.string().trim().min(1, "请输入空间名称。"); `country` z.string().length(2); `paymentChannelId` z.number().int().positive(); `currencyCode` z.string().length(3); `amountMinor` z.number().int().positive(); `openingDate` z.string().regex(/^\d{4}-\d{2}-\d{2}$/); `periodUnit` z.enum(["month","quarter","year"]); `periodCount` z.number().int().positive(); `motherEmail` z.string().trim().min(1, "请输入母账号邮箱/登录名。"). Export `type SpaceFormInput = z.infer<typeof spaceFormSchema>` and `spaceIdSchema = z.object({ id: z.number().int().positive() })`. Add the same doc-comment as channel.ts noting Server Actions re-parse server-side (ASVS V5) and parsing only known fields blocks mass-assignment.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'export const spaceFormSchema' src/lib/validation/space.ts` and `export const spaceIdSchema` both match.
    - `periodUnit` uses `z.enum(["month", "quarter", "year"])`.
    - `SpaceFormInput` type is exported via `z.infer`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Shared space validation schema + id schema + inferred type exist and compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DB data-access helpers (atomic write, list sort/filter, detail join, rate + minor-unit reads)</name>
  <read_first>
    - src/db/channels.ts (explicit-`db` helper convention, listChannels conditional where, findActiveByName single-row .get())
    - src/db/fxRates.ts (upsertRates db.transaction pattern + existing Db/FxRateRow types — add getRate here)
    - src/db/schema.ts (space, motherAccount, currency, paymentChannel columns)
    - .planning/phases/03-spaces-expiry-usd-snapshot/03-PATTERNS.md (spaces.ts lines 66-103; fxRates getRate lines 106-116; currencies.ts lines 120-134)
  </read_first>
  <files>src/db/spaces.ts, src/db/currencies.ts, src/db/fxRates.ts</files>
  <behavior>
    - insertSpaceWithMother(db, spaceValues, email) inserts the space (capturing the new id via .returning().get()) AND the mother_account row inside ONE synchronous db.transaction; returns the space row.
    - listSpaces(db, { country?, channelId? }) returns rows ordered by asc(space.expiryDate); applies eq filters only when each arg is present, combined with and(...).
    - getSpaceDetail(db, id) returns the space joined with motherAccount, paymentChannel, currency for one id (or undefined).
    - getRate(db, currencyCode) returns the single fx_rate row (rateToUsd + fetchedAt) or undefined.
    - getCurrencyMinorUnit(db, code) returns the integer minorUnit or undefined (JPY → 0).
  </behavior>
  <action>Create src/db/spaces.ts with `type Db = BetterSQLite3Database<Record<string, unknown>>` and `export type SpaceRow = typeof space.$inferSelect` (never import the @/db singleton). Implement `insertSpaceWithMother(db, spaceValues, email)` copying the SYNCHRONOUS `db.transaction((tx) => {...})` shape from fxRates.ts upsertRates (lines 42-54): `tx.insert(space).values(spaceValues).returning().get()` to capture the id, then `tx.insert(motherAccount).values({ spaceId: row.id, email }).run()`, return the space row — NO `await` inside the callback (Pitfall 2). Implement `listSpaces(db, { country, channelId })` using `db.select().from(space).where(and(country ? eq(space.country, country) : undefined, channelId ? eq(space.paymentChannelId, channelId) : undefined)).orderBy(asc(space.expiryDate)).all()` (import `and, asc, eq` from drizzle-orm; D-06 default sort). Implement `getSpaceDetail(db, id)` selecting the space row by id and joining motherAccount/paymentChannel/currency (leftJoin where appropriate), single-row via `.get()`. Also implement `updateSpaceRow(db, id, values)` (drizzle `.update(space).set(values).where(eq(space.id, id)).returning().get()`) for Plan 04's edit path. Create src/db/currencies.ts with `getCurrencyMinorUnit(db, code)` selecting `currency.minorUnit` by `eq(currency.code, code)` `.get()?.minorUnit` (resolves the freeze srcExp; JPY=0). Add `getRate(db, currencyCode)` to src/db/fxRates.ts alongside listRates, returning `db.select().from(fxRate).where(eq(fxRate.currencyCode, currencyCode)).get()` (import `eq`; reuse existing Db/FxRateRow). All queries use Drizzle parameterized builders — never string SQL.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/spaces.ts` exports `insertSpaceWithMother`, `listSpaces`, `getSpaceDetail`, `updateSpaceRow`, `SpaceRow`; uses `db.transaction((tx) =>` with no `await` inside.
    - `listSpaces` uses `orderBy(asc(space.expiryDate))` and conditional `and(...)` filters.
    - `src/db/currencies.ts` exports `getCurrencyMinorUnit`; `src/db/fxRates.ts` exports `getRate`.
    - No helper imports the `@/db` singleton (all take explicit `db`).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All five data helpers compile and follow the explicit-`db`, parameterized, synchronous-transaction conventions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DB-helper test — SPACE-02 sort/filter + ACCT-01 1:1/cascade</name>
  <read_first>
    - src/db/channels.query.test.ts (analog DB-helper test using createTestDb beforeEach/afterEach close)
    - src/test/db-harness.ts (createTestDb applies ./drizzle with foreign_keys=ON)
    - src/db/seed.ts (CURRENCY_SEED for seeding reference data) and src/lib/countries.ts (COUNTRIES codes)
  </read_first>
  <files>src/db/spaces.query.test.ts</files>
  <behavior>
    - listSpaces returns rows in expiry-ascending order by default.
    - listSpaces filtered by country returns only that country; filtered by channelId returns only that channel; both combined intersect.
    - insertSpaceWithMother writes both rows atomically; getSpaceDetail returns the joined mother email.
    - Inserting a second mother_account row for an existing space_id throws (DB unique constraint — ACCT-01 1:1).
    - The mother_account FK to space carries ON DELETE cascade (assert schema/migration; deleting a space removes its mother row).
  </behavior>
  <action>Create src/db/spaces.query.test.ts using `createTestDb()` in beforeEach and closing `ctx.sqlite` in afterEach (copy channels.query.test.ts structure). Seed currencies (CURRENCY_SEED) and at least two payment channels and spaces with differing expiryDate/country/paymentChannelId so sort + both filters are observable. Assert: default order is ascending by expiryDate; country filter narrows correctly; channel filter narrows correctly; insertSpaceWithMother creates space+mother in one call and getSpaceDetail returns the mother email; a direct second `insert(motherAccount)` with the same space_id rejects (expect throw); and deleting the space removes the mother row (cascade). Run until green.</action>
  <verify>
    <automated>npx vitest run src/db/spaces.query.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts expiry-ascending default order and both country/channel filters.
    - Test asserts a duplicate `space_id` mother insert throws (1:1 enforced by DB).
    - Test asserts cascade delete of mother row when its space is deleted.
    - `npx vitest run src/db/spaces.query.test.ts` passes.
  </acceptance_criteria>
  <done>DB-layer SPACE-02 and ACCT-01 behaviors are proven against the migrated in-memory schema.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- **Symbols (validation):** `spaceFormSchema`, `spaceIdSchema`, type `SpaceFormInput` in `src/lib/validation/space.ts`.
- **Symbols (db/spaces.ts):** `insertSpaceWithMother(db, spaceValues, email)`, `listSpaces(db, { country?, channelId? })`, `getSpaceDetail(db, id)`, `updateSpaceRow(db, id, values)`, type `SpaceRow`.
- **Symbol (db/currencies.ts):** `getCurrencyMinorUnit(db, code): number | undefined`.
- **Symbol (db/fxRates.ts):** `getRate(db, currencyCode): FxRateRow | undefined` (added).
- **File:** `src/db/spaces.query.test.ts` (new).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → SQLite | All space/mother reads & writes cross here; parameterized queries only |
| Reference data → space row | country/channel/currency must reference valid seeded data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | SQL injection in spaces/currencies/fxRates helpers | high | mitigate | Drizzle parameterized builders only; no string-concatenated SQL anywhere in the new helpers. |
| T-03-07 | Tampering / Integrity | duplicate or orphaned mother account | high | mitigate | Single synchronous `db.transaction` for the 2-table write + DB UNIQUE on `space_id`; proven by the duplicate-insert-throws test. |
| T-03-08 | Tampering | `await` inside better-sqlite3 transaction breaking atomicity | medium | mitigate | Transaction callback is synchronous; all async work (rate refresh) happens in Plan 04 before the txn. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for the new/modified data + validation files.
- `npx vitest run src/db/spaces.query.test.ts` passes (sort, filters, 1:1 reject, cascade).
- Source review: no `@/db` singleton import in `db/*` helpers; no string SQL.
</verification>

<success_criteria>
The space validation schema and parameterized data layer (atomic write, expiry-asc sort + country/channel filter, detail join, rate + minor-unit reads) exist and are proven at the DB layer for SPACE-02 and ACCT-01.
</success_criteria>

<output>
Create `.planning/phases/03-spaces-expiry-usd-snapshot/03-03-SUMMARY.md` when done.
</output>
