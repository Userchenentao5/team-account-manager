---
phase: 05-dashboard-overview
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-02
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
requirements_addressed:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "The completed dashboard passes targeted query tests, full Vitest suite, lint, and production build."
    - "The completed dashboard satisfies DASH-01, DASH-02, DASH-03, and DASH-04 in the browser."
    - "The completed dashboard satisfies D-01 through D-05 from the Phase 5 context."
    - "Validation backstops confirm no chart package, no live FX recomputation, no dashboard mutation controls, and no schema/migration scope."
  artifacts:
    - path: ".planning/phases/05-dashboard-overview/05-03-SUMMARY.md"
      provides: "Final Phase 5 automated and manual verification summary"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/db/dashboard.ts"
      via: "getDashboardOverview(db)"
      pattern: "getDashboardOverview\\(db\\)"
    - from: "src/db/dashboard.query.test.ts"
      to: "src/db/dashboard.ts"
      via: "targeted aggregate coverage"
      pattern: "getDashboardOverview"
---

<objective>
Run the final Phase 5 verification gate and capture the required manual dashboard check.

Purpose: The data and UI plans are complete only when the full app gate passes and the dashboard visually matches the approved compact operations-dashboard contract.
Output: Automated verification results, source/package backstop checks, human visual verification checkpoint, and final summary.
</objective>

## Phase Goal

**As a** team subscription manager, **I want to** open the dashboard and see renewal risk, frozen USD cost, distributions, and counts at a glance, **so that** I can prioritize renewals and understand the cost footprint without recomputing history.

<execution_context>
@C:/Users/30952/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/30952/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-dashboard-overview/05-CONTEXT.md
@.planning/phases/05-dashboard-overview/05-RESEARCH.md
@.planning/phases/05-dashboard-overview/05-PATTERNS.md
@.planning/phases/05-dashboard-overview/05-VALIDATION.md
@.planning/phases/05-dashboard-overview/05-UI-SPEC.md
@.planning/phases/05-dashboard-overview/05-01-SUMMARY.md
@.planning/phases/05-dashboard-overview/05-02-SUMMARY.md
@src/db/dashboard.ts
@src/db/dashboard.query.test.ts
@src/app/page.tsx
@src/components/dashboard/metric-card.tsx
@src/components/dashboard/expiring-space-table.tsx
@src/components/dashboard/distribution-list.tsx
@package.json
</context>

## Artifacts this phase produces

- `05-03-SUMMARY.md`
  - Final automated gate results
  - Source/package assertion results
  - Human visual verification notes for `/`

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 5 | Dashboard delivers renewal and cost overview at a glance | 05-03 | COVERED | Final full gate and browser checkpoint validate the implemented result. |
| REQ | DASH-01 | Expired/soon spaces highlighted | 05-03 | COVERED | Browser checkpoint verifies the risk section and query tests remain green. |
| REQ | DASH-02 | Total USD spend | 05-03 | COVERED | Automated and manual checks verify grand total plus subtotals. |
| REQ | DASH-03 | Spend distributions | 05-03 | COVERED | Source/package and browser checks verify CSS bar-list distributions. |
| REQ | DASH-04 | Count overview | 05-03 | COVERED | Browser checkpoint verifies space and child counts. |
| CONTEXT | D-01 | Renewal risk and total USD together | 05-03 | COVERED | Manual first-viewport check. |
| CONTEXT | D-02 | Compact operations layout | 05-03 | COVERED | Manual desktop/mobile visual check. |
| CONTEXT | D-03 | Four metric order | 05-03 | COVERED | Manual and source-backed check. |
| CONTEXT | D-04 | Expiring list before distributions | 05-03 | COVERED | Manual and source-backed check. |
| CONTEXT | D-05 | View-first dashboard | 05-03 | COVERED | Source assertion rejects dashboard mutation wiring. |
| RESEARCH | R-01 | No live FX recomputation | 05-03 | COVERED | Source assertion checks dashboard aggregate module for live FX/recompute imports. |
| RESEARCH | R-03 | No chart package | 05-03 | COVERED | Package assertion checks chart packages are absent. |
| VALIDATION | V-01 | Targeted, full, lint, build, and manual checks | 05-03 | COVERED | This plan is the phase gate. |
| UI-SPEC | UI-01 | First viewport, copy, responsive layout, and no mutation controls | 05-03 | COVERED | Human checkpoint verifies desktop and mobile. |

<tasks>

<task type="auto">
  <name>Task 1: Run final automated and source backstop gates</name>
  <files>src/db/dashboard.ts, src/db/dashboard.query.test.ts, src/app/page.tsx, src/components/dashboard/metric-card.tsx, src/components/dashboard/expiring-space-table.tsx, src/components/dashboard/distribution-list.tsx, package.json</files>
  <read_first>
    @.planning/phases/05-dashboard-overview/05-VALIDATION.md
    @.planning/phases/05-dashboard-overview/05-RESEARCH.md
    @.planning/phases/05-dashboard-overview/05-UI-SPEC.md
    @src/db/dashboard.ts
    @src/db/dashboard.query.test.ts
    @src/app/page.tsx
    @package.json
  </read_first>
  <action>Run the final automated gate after Plans 05-01 and 05-02 are complete. Use the exact Vitest commands from `05-VALIDATION.md`; do not use Jest-only flags. Then run source/package assertions for the research and UI backstops: no chart dependency, no dashboard live FX recomputation, no dashboard mutation controls, and no schema/migration files listed in Phase 5 `files_modified`. If an automated command fails because of implementation behavior, fix the relevant Phase 5 implementation file and rerun the targeted command before rerunning the full gate.</action>
  <acceptance_criteria>
    - `npm test -- src/db/dashboard.query.test.ts` passes.
    - `npm test` passes.
    - `npm run lint` passes.
    - `npm run build` passes.
    - `package.json` has no chart-package dependency added for Phase 5.
    - `src/db/dashboard.ts` does not import live FX refresh/read helpers or money-freeze conversion helpers for dashboard totals.
    - `src/app/page.tsx` does not contain dashboard-specific create, edit, delete, form, or Server Action wiring.
    - The Phase 5 plan set does not list `src/db/schema.ts`, `drizzle/*.sql`, or `drizzle/meta/*.json` in `files_modified`; no schema push/migration task is required.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/db/dashboard.query.test.ts</automated>
    <automated>npm test</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
    <automated>powershell -NoProfile -Command "`$pkg = Get-Content -Raw 'package.json'; if (`$pkg -match 'recharts|chart\\.js|@nivo|victory') { throw 'chart dependency detected' }; `$dashboard = Get-Content -Raw 'src/db/dashboard.ts'; if (`$dashboard -match 'ensureFreshRates|refreshFromApi|getRate\\(|freezeUsdMinor|convertUsdMinorToCurrencyMinor') { throw 'dashboard aggregate live FX or recompute path detected' }; `$page = Get-Content -Raw 'src/app/page.tsx'; if (`$page -match 'useActionState|useForm|createSpace|updateSpace|deleteSpace|archive|reactivate') { throw 'dashboard mutation wiring detected' }; `$planPaths = @('.planning/phases/05-dashboard-overview/05-01-PLAN.md', '.planning/phases/05-dashboard-overview/05-02-PLAN.md', '.planning/phases/05-dashboard-overview/05-03-PLAN.md'); foreach (`$path in `$planPaths) { `$inFiles = `$false; foreach (`$line in Get-Content `$path) { if (`$line -match '^files_modified:') { `$inFiles = `$true; continue }; if (`$inFiles -and `$line -match '^[a-zA-Z_]+:') { `$inFiles = `$false }; if (`$inFiles -and `$line -match 'src/db/schema\\.ts|drizzle/') { throw 'Phase 5 files_modified unexpectedly lists schema or migration files' } } }"</automated>
  </verify>
  <done>All automated Phase 5 gates and source backstops pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the dashboard visually in browser</name>
  <files>src/app/page.tsx, src/components/dashboard/metric-card.tsx, src/components/dashboard/expiring-space-table.tsx, src/components/dashboard/distribution-list.tsx</files>
  <read_first>
    @.planning/phases/05-dashboard-overview/05-UI-SPEC.md
    @.planning/phases/05-dashboard-overview/05-VALIDATION.md
    @src/app/page.tsx
    @src/components/dashboard/metric-card.tsx
    @src/components/dashboard/expiring-space-table.tsx
    @src/components/dashboard/distribution-list.tsx
  </read_first>
  <action>After Task 1 passes, start the local Next.js dev server if one is not already running, open `/`, and verify the approved UI contract in a real browser. Check a desktop viewport and a narrow mobile viewport. The dashboard should be the first screen, not a landing page; it should show metric cards first, the renewal-risk section immediately after, then spend distributions and count overview. Links may navigate to existing space detail/list flows, but the dashboard itself must remain view-first per D-05.</action>
  <acceptance_criteria>
    - Desktop first viewport shows the page heading, four metric cards in D-03 order, and the start of the expiring-space section.
    - Renewal-risk metric and frozen USD total are both visible together, satisfying D-01.
    - Layout is compact and operational, satisfying D-02; no decorative hero, large marketing copy, gradient/orb background, or card-in-card section layout appears.
    - Expiring/expired rows show tiered status labels and appear before distributions, satisfying DASH-01 and D-04.
    - Grand total, space payment subtotal, and child monthly subtotal are visible and reconcile, satisfying DASH-02.
    - Country, currency, and payment-channel distributions are readable CSS bar lists without chart-package visuals, satisfying DASH-03.
    - Count overview shows space and child-account counts, satisfying DASH-04.
    - Dashboard has no create/edit/delete buttons, mutation dialogs, or dashboard-specific forms, satisfying D-05.
    - Mobile view has no incoherent text overlap in metric cards, table overflow, buttons, or bars.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <human-check>Open `http://localhost:3000/` or the active dev-server URL, inspect desktop and mobile widths, and confirm every acceptance criterion above.</human-check>
  </verify>
  <done>Human visual verification approves the Phase 5 dashboard against the UI contract.</done>
  <resume-signal>Type "approved" or describe the dashboard issues that need correction.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated source gate to release readiness | Source assertions decide whether the dashboard implementation is safe to verify manually. |
| Browser UI to user decision-making | Rendered totals and status ordering influence renewal and cost decisions. |
| Package manager boundary | Final package audit verifies no unplanned dashboard dependency was added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-TAMPER | Tampering | Final dashboard totals and source gates | medium | mitigate | Full query tests plus source assertions block live FX recomputation, join double-counting regressions, and mutation wiring. |
| T-05-03-INFO | Information Disclosure | Browser dashboard | medium | mitigate | Human check verifies the UI displays only approved operational data and no secret-like credential fields or stack/database paths. |
| T-05-03-DOS | Denial of Service | Dashboard responsive layout | low | mitigate | Browser check covers mobile overflow and fixed bar/table sizing; production build must pass. |
| T-05-03-SC | Tampering | npm installs | low | accept | Final package assertion confirms no chart or unplanned package dependency was introduced. |
</threat_model>

<verification>
Plan-level checks:
- `npm test -- src/db/dashboard.query.test.ts`
- `npm test`
- `npm run lint`
- `npm run build`
- Source/package assertion for no chart package, no live FX recomputation, no dashboard mutation controls, and no schema/migration scope
- Human browser verification for desktop and mobile

No schema-relevant files are modified by the Phase 5 plan set, so the schema push/migration gate is intentionally not triggered.
</verification>

<success_criteria>
Phase 5 is complete when automated checks pass, source backstops pass, the browser dashboard satisfies the Phase 5 UI contract, and the human checkpoint is approved.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-overview/05-03-SUMMARY.md` when done.
</output>
