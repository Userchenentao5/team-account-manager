---
phase: 05-dashboard-overview
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/page.tsx
  - src/components/dashboard/metric-card.tsx
  - src/components/dashboard/expiring-space-table.tsx
  - src/components/dashboard/distribution-list.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
requirements_addressed:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "D-01: the first screen shows renewal risk and total frozen USD spend together."
    - "D-02: the dashboard uses compact operations-dashboard composition, not a marketing landing page."
    - "D-03: the four top metrics appear in this exact order: renewal-risk spaces, total frozen USD spend, total spaces, total child accounts."
    - "D-04: the expiring-space list appears immediately after the top metrics and before distribution/count sections."
    - "D-05: the dashboard is read-only and links to existing space detail/list flows rather than adding dashboard mutation workflows."
    - "DASH-03: country, currency, and payment-channel distributions render as CSS bar lists without a chart package."
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Root dynamic dashboard page wired to getDashboardOverview(db)"
      exports:
        - dynamic
        - DashboardPage
    - path: "src/components/dashboard/metric-card.tsx"
      provides: "Compact metric card for top dashboard metrics"
      exports:
        - MetricCard
    - path: "src/components/dashboard/expiring-space-table.tsx"
      provides: "Read-only expiring/expired spaces table with detail links"
      exports:
        - ExpiringSpaceTable
    - path: "src/components/dashboard/distribution-list.tsx"
      provides: "Spend bucket CSS bar-list component"
      exports:
        - DistributionList
  key_links:
    - from: "src/app/page.tsx"
      to: "src/db/dashboard.ts"
      via: "getDashboardOverview(db)"
      pattern: "getDashboardOverview\\(db"
    - from: "src/app/page.tsx"
      to: "src/components/dashboard/expiring-space-table.tsx"
      via: "overview.expiringSpaces prop"
      pattern: "ExpiringSpaceTable"
    - from: "src/components/dashboard/expiring-space-table.tsx"
      to: "src/components/spaces/expiry-badge.tsx"
      via: "ExpiryBadge for stored expiryDate status display"
      pattern: "ExpiryBadge"
---

<objective>
Replace the placeholder root page with the real read-only dashboard slice.

Purpose: Users should get the Phase 5 core value immediately at `/`: renewal risk, frozen USD footprint, distributions, and inventory counts in the compact operations style specified by the UI contract.
Output: Dynamic root dashboard page and focused dashboard components using the tested aggregate DTO from Plan 05-01.
</objective>

## Phase Goal

**As a** team subscription manager, **I want to** open the dashboard and see renewal risk, frozen USD cost, distributions, and counts at a glance, **so that** I can prioritize renewals and understand the cost footprint without recomputing history.

<execution_context>
@C:/Users/30952/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/30952/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-dashboard-overview/05-CONTEXT.md
@.planning/phases/05-dashboard-overview/05-RESEARCH.md
@.planning/phases/05-dashboard-overview/05-PATTERNS.md
@.planning/phases/05-dashboard-overview/05-VALIDATION.md
@.planning/phases/05-dashboard-overview/05-UI-SPEC.md
@.planning/phases/05-dashboard-overview/05-01-SUMMARY.md
@src/app/page.tsx
@src/app/spaces/page.tsx
@src/db/dashboard.ts
@src/components/spaces/space-table.tsx
@src/components/spaces/expiry-badge.tsx
@src/components/ui/card.tsx
@src/components/ui/table.tsx
@src/components/ui/button.tsx
@src/components/ui/tooltip.tsx
@src/lib/money.ts
</context>

## Artifacts this phase produces

- `src/app/page.tsx`
  - `export const dynamic = "force-dynamic"`
  - `DashboardPage()` server component wired to `getDashboardOverview(db)`
- `src/components/dashboard/metric-card.tsx`
  - `MetricCard`
- `src/components/dashboard/expiring-space-table.tsx`
  - `ExpiringSpaceTable`
- `src/components/dashboard/distribution-list.tsx`
  - `DistributionList`

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 5 | Dashboard gives renewal and cost overview at a glance | 05-02 | COVERED | This plan replaces `/` with the visible dashboard. |
| REQ | DASH-01 | Expired/soon spaces highlighted | 05-02 | COVERED | Expiring table renders the Plan 05-01 risk rows. |
| REQ | DASH-02 | Total frozen USD spend | 05-02 | COVERED | Metric card displays grand total and subtotals. |
| REQ | DASH-03 | Spend distribution | 05-02 | COVERED | DistributionList renders country/currency/payment-channel CSS bars. |
| REQ | DASH-04 | Count overview | 05-02 | COVERED | Metric cards and secondary count section render totals and status/seat counts. |
| CONTEXT | D-01 | Renewal risk and total USD together | 05-02 | COVERED | First screen combines both metric cards. |
| CONTEXT | D-02 | Compact operations layout | 05-02 | COVERED | Uses dense cards, table, badges, mono numbers, and CSS bars. |
| CONTEXT | D-03 | Four key metrics in exact order | 05-02 | COVERED | Task 1 renders that order. |
| CONTEXT | D-04 | Expiring list before distributions | 05-02 | COVERED | Task 1 places the risk list directly after metrics. |
| CONTEXT | D-05 | View-first dashboard | 05-02 | COVERED | Links only; no dashboard create/edit/delete actions. |
| RESEARCH | R-01 | Dynamic RSC for SQLite reads | 05-02 | COVERED | Root page exports `force-dynamic`. |
| RESEARCH | R-03 | No chart dependency | 05-02, 05-03 | COVERED | CSS bar-list component only. |
| UI-SPEC | UI-01 | Exact layout, typography, spacing, and copy contract | 05-02, 05-03 | COVERED | This plan implements; Plan 05-03 verifies visually. |
| VALIDATION | V-02 | UI source/build checks and manual visual checks | 05-02, 05-03 | COVERED | This plan runs build/lint; Plan 05-03 runs final and human checks. |

<tasks>

<task type="auto">
  <name>Task 1: Build the root dashboard metrics and renewal-risk slice</name>
  <files>src/app/page.tsx, src/components/dashboard/metric-card.tsx, src/components/dashboard/expiring-space-table.tsx</files>
  <read_first>
    @.planning/phases/05-dashboard-overview/05-CONTEXT.md
    @.planning/phases/05-dashboard-overview/05-UI-SPEC.md
    @.planning/phases/05-dashboard-overview/05-PATTERNS.md
    @.planning/phases/05-dashboard-overview/05-01-SUMMARY.md
    @src/app/page.tsx
    @src/app/spaces/page.tsx
    @src/db/dashboard.ts
    @src/components/spaces/space-table.tsx
    @src/components/spaces/expiry-badge.tsx
    @src/components/ui/card.tsx
    @src/components/ui/table.tsx
    @src/components/ui/button.tsx
    @src/components/ui/tooltip.tsx
    @src/lib/money.ts
  </read_first>
  <action>Replace the `src/app/page.tsx` placeholder with a Node-backed Server Component that imports `db` and `getDashboardOverview`, exports `dynamic = "force-dynamic"`, and renders a compact root dashboard. Add `MetricCard` for the top metric grid and `ExpiringSpaceTable` for the read-only renewal-risk table. Follow D-01, D-02, D-03, D-04, and D-05 exactly: page heading and subtitle from `05-UI-SPEC.md`, four metrics in the required order, renewal-risk count first, frozen USD total second with `spacePaymentUsdMinor` and `childMonthlyUsdMinor` subtotals, total spaces third, total child accounts fourth, then the expiring-space section before any distribution content. If there are zero spaces, render the overall dashed empty state from the UI spec after the heading rather than four zero metric cards. If renewal-risk count is greater than zero, render the primary `查看到期空间` CTA as an anchor to the expiring section. The risk table columns are status, space name, expiry date, days remaining or overdue, frozen USD cost, child-account count, payment channel, and action. The space name and `Eye` icon both link to `/spaces/[id]` with tooltip and aria label `查看空间详情`; do not add edit, delete, create, or renewal mutation controls.</action>
  <acceptance_criteria>
    - `/` reads through `getDashboardOverview(db)` and exports `dynamic = "force-dynamic"`.
    - The metric grid order matches D-03 and the UI spec: renewal risk, frozen USD cost total, total spaces, total child accounts.
    - The expiring-space list appears directly after the metrics and before distribution/count sections, satisfying D-04.
    - Expiring rows reuse `ExpiryBadge` and link only to existing `/spaces/[id]` detail pages.
    - The root dashboard contains no form, Server Action, edit dialog, delete dialog, or inline mutation control, satisfying D-05.
    - The zero-space state uses the UI-spec empty copy and does not show four zero metric cards above it.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
    <automated>powershell -NoProfile -Command "`$page = Get-Content -Raw 'src/app/page.tsx'; if (`$page -notmatch 'force-dynamic') { throw 'root dashboard must be force-dynamic' }; if (`$page -notmatch 'getDashboardOverview\\(db\\)') { throw 'root dashboard must call getDashboardOverview(db)' }; if (`$page -match 'useActionState|useForm|deleteSpace|createSpace|updateSpace') { throw 'dashboard mutation wiring detected' }; `$risk = Get-Content -Raw 'src/components/dashboard/expiring-space-table.tsx'; if (`$risk -notmatch 'ExpiryBadge') { throw 'expiring table must reuse ExpiryBadge' }; if (`$risk -notmatch 'Eye') { throw 'expiring table must include Eye detail link' }"</automated>
  </verify>
  <done>The root page renders the first-screen dashboard metrics and renewal-risk table from the tested aggregate DTO.</done>
</task>

<task type="auto">
  <name>Task 2: Add spend distributions and secondary count overview</name>
  <files>src/app/page.tsx, src/components/dashboard/distribution-list.tsx</files>
  <read_first>
    @.planning/phases/05-dashboard-overview/05-UI-SPEC.md
    @.planning/phases/05-dashboard-overview/05-RESEARCH.md
    @src/app/page.tsx
    @src/db/dashboard.ts
    @src/components/ui/card.tsx
    @src/components/ui/separator.tsx
    @src/lib/money.ts
    @package.json
  </read_first>
  <action>Add `DistributionList` as a presentational component for spend buckets and wire three instances into `src/app/page.tsx`: country, currency, and payment channel. Each row shows label, monospaced USD total, percentage of the grand total, and a fixed-height CSS bar. Use existing shadcn/Radix primitives and Tailwind classes only; do not add a chart package or registry block. Add the secondary count overview after the distribution section, showing total spaces, expired spaces, soon spaces, normal spaces, total child accounts, and child-account counts by `seatType` when present. Keep all distribution and count sections below the expiring-space section to preserve D-04. Show the UI-spec distribution empty state when there is no spend data, and show a reconciliation footer only when the integer bucket total equals the dashboard grand total.</action>
  <acceptance_criteria>
    - Three distribution cards render in the required dimensions: country, currency, payment channel.
    - Distribution bucket rows display USD amount, percentage, and CSS bar without importing any chart library.
    - Bucket footer copy only claims reconciliation when the integer bucket total equals the grand total.
    - Count overview remains secondary to the expiring-space table and includes status counts plus child seat-type counts.
    - `package.json` dependencies and devDependencies remain unchanged for Phase 5 dashboard UI.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
    <automated>powershell -NoProfile -Command "`$pkg = Get-Content -Raw 'package.json'; if (`$pkg -match 'recharts|chart\\.js|@nivo|victory') { throw 'chart dependency detected' }; `$dist = Get-Content -Raw 'src/components/dashboard/distribution-list.tsx'; if (`$dist -notmatch 'height|h-2') { throw 'distribution bars must have stable fixed height' }; `$page = Get-Content -Raw 'src/app/page.tsx'; if (`$page -notmatch 'DistributionList') { throw 'dashboard must render distribution lists' }"</automated>
  </verify>
  <done>The root dashboard shows spend distribution and count overview below the renewal-risk section without new dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server DTO to browser UI | Aggregate cost, count, and risk data render into browser-visible dashboard components. |
| Dashboard navigation to existing routes | Dashboard links users into existing `/spaces/[id]` detail flows. |
| Package manager boundary | UI must use existing vendored primitives and no new chart package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-INFO | Information Disclosure | Dashboard components | medium | mitigate | Render only existing space names, payment channels, costs, counts, and links; do not render credentials, secrets, tokens, or hidden database paths. |
| T-05-02-TAMPER | Tampering | Root dashboard route | low | mitigate | Keep the route read-only and omit forms, Server Actions, edit/delete buttons, and dashboard mutation controls per D-05. |
| T-05-02-DOS | Denial of Service | Responsive dashboard tables and bars | low | mitigate | Use horizontal overflow for tables, stable fixed-height bars, and compact empty states so data volume or percentages do not break layout. |
| T-05-02-SC | Tampering | npm installs | low | accept | No package install or upgrade task exists; CSS bars use existing UI primitives. |
</threat_model>

<verification>
Plan-level checks:
- `npm run lint`
- `npm run build`
- Source assertion that root `/` is force-dynamic and wired to `getDashboardOverview(db)`
- Source/package assertion that no dashboard mutation wiring or chart dependency is introduced

No schema-relevant files are modified by this plan, so the schema push/migration gate is intentionally not triggered.
</verification>

<success_criteria>
The root dashboard replaces the placeholder, displays the Phase 5 metric hierarchy and renewal-risk list first, renders distributions and counts below it, stays read-only, uses stored aggregate DTO values, and adds no new package dependencies.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-overview/05-02-SUMMARY.md` when done.
</output>
