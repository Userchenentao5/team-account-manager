---
phase: 05-dashboard-overview
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/db/dashboard.ts
  - src/db/dashboard.query.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
requirements_addressed:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "DASH-01: expired and soon-to-expire spaces are counted and returned in expired-first, expiry-date order for the dashboard renewal-risk list, supporting D-01 and D-04."
    - "DASH-02: total frozen USD cost is computed from stored integer USD fields only: space.amountUsd plus childAccount.monthlyAmountUsd."
    - "DASH-03: country, currency, and payment-channel spend buckets each reconcile exactly to the dashboard grand total in integer minor units."
    - "DASH-04: overview counts include total spaces, total child accounts, expiry-status counts, and child account counts by seatType."
    - "The aggregate facade does not read live FX cache rows or recompute historical USD values."
  artifacts:
    - path: "src/db/dashboard.ts"
      provides: "getDashboardOverview(db, today?) facade and exported dashboard DTO types"
      exports:
        - getDashboardOverview
        - DashboardOverview
        - DashboardExpiringSpaceRow
        - DashboardSpendBucket
    - path: "src/db/dashboard.query.test.ts"
      provides: "Vitest coverage for DASH-01 through DASH-04, double-counting guard, and empty-state aggregate behavior"
  key_links:
    - from: "src/db/dashboard.ts"
      to: "src/db/schema.ts"
      via: "Drizzle aggregate reads over space, childAccount, paymentChannel, and currency"
      pattern: "from\\((space|childAccount)\\)"
    - from: "src/db/dashboard.query.test.ts"
      to: "src/test/db-harness.ts"
      via: "createTestDb() migration-backed in-memory SQLite harness"
      pattern: "createTestDb\\("
    - from: "src/db/dashboard.ts"
      to: "src/lib/expiry.ts"
      via: "expiryStatus(expiryDate, today) for status counts and risk filtering"
      pattern: "expiryStatus\\("
---

<objective>
Create the tested dashboard data contract that the root dashboard will render.

Purpose: Phase 5 must expose the core value using stored derived fields, so the first implementation boundary is a reliable aggregate facade that cannot double-count parent payments or recompute historical FX.
Output: `getDashboardOverview(db, today?)`, dashboard DTO types, and focused Vitest coverage for DASH-01, DASH-02, DASH-03, and DASH-04.
</objective>

## Phase Goal

**As a** team subscription manager, **I want to** open the dashboard and see renewal risk, frozen USD cost, distributions, and counts at a glance, **so that** I can prioritize renewals and understand the cost footprint without recomputing history.

<execution_context>
@C:/Users/30952/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/30952/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-dashboard-overview/05-CONTEXT.md
@.planning/phases/05-dashboard-overview/05-RESEARCH.md
@.planning/phases/05-dashboard-overview/05-PATTERNS.md
@.planning/phases/05-dashboard-overview/05-VALIDATION.md
@src/db/schema.ts
@src/db/spaces.ts
@src/db/childAccounts.ts
@src/db/spaces.query.test.ts
@src/db/childAccounts.query.test.ts
@src/test/db-harness.ts
@src/lib/expiry.ts
@src/lib/money.ts
</context>

## Artifacts this phase produces

- `src/db/dashboard.ts`
  - `getDashboardOverview(db, today?)`
  - `DashboardOverview`
  - `DashboardExpiringSpaceRow`
  - `DashboardSpendBucket`
  - `DashboardCountSummary`
- `src/db/dashboard.query.test.ts`
  - Query tests for renewal-risk ordering, total/subtotal reconciliation, distribution buckets, count buckets, empty data, and parent-payment double-counting protection.

## Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 5 | Core-value dashboard over renewal risk and frozen USD spend | 05-01, 05-02, 05-03 | COVERED | Data contract in this plan, UI and verification later. |
| REQ | DASH-01 | Expired and soon-expiring spaces highlighted | 05-01, 05-02, 05-03 | COVERED | This plan owns query status counts and risk list ordering. |
| REQ | DASH-02 | Total USD spend | 05-01, 05-02, 05-03 | COVERED | This plan owns stored-field integer totals and subtotal reconciliation. |
| REQ | DASH-03 | Spend distribution by country, currency, and payment channel | 05-01, 05-02, 05-03 | COVERED | This plan owns bucket DTOs and reconciliation tests. |
| REQ | DASH-04 | Space and child-account counts | 05-01, 05-02, 05-03 | COVERED | This plan owns count DTOs and tests. |
| CONTEXT | D-01 | Renewal risk and total USD spend visible together | 05-02 | COVERED | Aggregate supplies both values to the first screen. |
| CONTEXT | D-02 | Compact operations dashboard | 05-02 | COVERED | UI plan applies compact shadcn/Radix layout. |
| CONTEXT | D-03 | Four metric order | 05-02 | COVERED | UI plan renders the exact order. |
| CONTEXT | D-04 | Expiring list immediately after metrics | 05-02 | COVERED | UI plan places the risk table before distributions. |
| CONTEXT | D-05 | View-first dashboard | 05-02, 05-03 | COVERED | UI and final gates reject dashboard mutation workflows. |
| RESEARCH | R-01 | Aggregate stored derived fields; no per-row live FX recomputation | 05-01, 05-03 | COVERED | This plan implements and tests stored-field aggregation. |
| RESEARCH | R-02 | Avoid join double-counting | 05-01 | COVERED | Tests seed one space with multiple children and assert the parent payment is counted once. |
| RESEARCH | R-03 | No new packages or chart engine | 05-02, 05-03 | COVERED | UI uses CSS bars and final source/package gates. |
| VALIDATION | V-01 | Held-out/backstop aggregate checks | 05-01, 05-03 | COVERED | Targeted Vitest file covers all DASH IDs and final gate reruns full checks. |
| UI-SPEC | UI-01 | Metric order, copy, expiring priority, no mutation controls | 05-02, 05-03 | COVERED | UI plan plus human verification plan cover visual contract. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing dashboard aggregate tests for DASH-01 through DASH-04</name>
  <files>src/db/dashboard.query.test.ts</files>
  <read_first>
    @.planning/phases/05-dashboard-overview/05-CONTEXT.md
    @.planning/phases/05-dashboard-overview/05-RESEARCH.md
    @.planning/phases/05-dashboard-overview/05-VALIDATION.md
    @.planning/phases/05-dashboard-overview/05-PATTERNS.md
    @src/db/spaces.query.test.ts
    @src/db/childAccounts.query.test.ts
    @src/test/db-harness.ts
    @src/db/schema.ts
    @src/lib/expiry.ts
  </read_first>
  <behavior>
    - DASH-01: with `today` fixed, expired rows and rows expiring within 7 calendar days are included, normal rows are excluded, and ordering is expired first then `expiryDate` ascending.
    - DASH-02: `spacePaymentUsdMinor + childMonthlyUsdMinor === totalUsdMinor`, using stored `space.amountUsd` and `childAccount.monthlyAmountUsd`.
    - DASH-03: country, currency, and payment-channel bucket totals each sum to `totalUsdMinor`.
    - DASH-04: total spaces, child accounts, expiry status counts, and `codex` / `chatgpt` child counts are returned.
    - Backstop: adding multiple child rows under one space must not multiply that space's parent `amountUsd`.
  </behavior>
  <action>Create `src/db/dashboard.query.test.ts` using the existing Vitest + `createTestDb()` pattern. Seed currencies, create payment channels, insert spaces through existing explicit-db helpers where useful, and insert child accounts through `insertChildAccount`. Import `getDashboardOverview` from `src/db/dashboard.ts` even though it does not exist yet; that failing import is the RED step. Keep the test data intentionally small and deterministic. Cite DASH-01, DASH-02, DASH-03, DASH-04, D-01, D-03, and D-04 in test descriptions where helpful for traceability.</action>
  <acceptance_criteria>
    - The targeted command fails before Task 2 because the dashboard module or exported function is absent.
    - Tests include a fixed `today` date and do not depend on the machine clock.
    - Tests assert exact integer minor-unit totals, not formatted strings.
    - Tests include a double-counting guard with at least one space that has two child accounts.
    - Tests include an empty database case that returns stable zero totals and empty lists.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/db/dashboard.query.test.ts</automated>
  </verify>
  <done>Dashboard aggregate tests exist and fail for the missing production contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement getDashboardOverview with stored-field aggregation</name>
  <files>src/db/dashboard.ts, src/db/dashboard.query.test.ts</files>
  <read_first>
    @src/db/dashboard.query.test.ts
    @src/db/schema.ts
    @src/db/spaces.ts
    @src/db/childAccounts.ts
    @src/db/channels.ts
    @src/lib/expiry.ts
    @src/lib/money.ts
    @.planning/phases/05-dashboard-overview/05-RESEARCH.md
    @.planning/phases/05-dashboard-overview/05-PATTERNS.md
  </read_first>
  <behavior>
    - `getDashboardOverview(db, today?)` returns one DTO containing metric totals, expiring rows, spend buckets, and count summaries.
    - Space payment totals and child monthly totals are read separately and merged in TypeScript to avoid one-to-many join multiplication.
    - Currency buckets attribute space payment cost to `space.currencyCode` and child monthly cost to `childAccount.monthlyCurrencyCode`.
    - Country and payment-channel buckets attribute both space and child cost to the owning parent space country/channel.
    - Null `space.amountUsd` values are treated as zero for dashboard totals without inventing replacement FX rates.
  </behavior>
  <action>Add `src/db/dashboard.ts` following the explicit `db: Db` helper style from `src/db/spaces.ts` and `src/db/childAccounts.ts`. Define exported DTO types near the facade. Use Drizzle builders and typed SQL aggregates where helpful, but do not concatenate SQL strings. Reuse `expiryStatus(expiryDate, today)` for status classification. Aggregate `space.amountUsd` and `childAccount.monthlyAmountUsd` separately, merge bucket maps using integer USD minor units, compute percentages as derived DTO display helpers, and return reconciliation fields that the UI can show directly. Do not import live FX services, do not call refresh actions, and do not write schema or migration files. This implements the stored-derived-field mandate for DASH-01, DASH-02, DASH-03, DASH-04, D-01, and D-05.</action>
  <acceptance_criteria>
    - `npm test -- src/db/dashboard.query.test.ts` passes.
    - Changing only child-account count does not change `spacePaymentUsdMinor`.
    - Every distribution dimension reports `sum(bucket.usdMinor) === totalUsdMinor`.
    - The facade exports DTO types usable by `src/app/page.tsx` without importing database tables into dashboard components.
    - The implementation does not modify `src/db/schema.ts`, `drizzle/*.sql`, or `drizzle/meta/*.json`; Phase 5 has no schema push/migration gate.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/db/dashboard.query.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`getDashboardOverview` is implemented and all dashboard aggregate tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite data to dashboard DTO | Persisted local rows become summarized dashboard output. |
| Aggregate query facade to UI | Server DTOs will cross into root page rendering in a later plan. |
| Package manager boundary | No npm install or upgrade is required for this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-TAMPER | Tampering | `getDashboardOverview` totals | medium | mitigate | Aggregate parent space payments and child monthly costs separately; test double-counting and exact integer reconciliation. |
| T-05-01-INFO | Information Disclosure | Dashboard DTO fields | medium | mitigate | Return cost/count/risk fields and existing space display metadata only; do not include password/token/secret fields or child account credentials. |
| T-05-01-DOS | Denial of Service | Empty or partial data sets | low | mitigate | Coalesce missing sums to zero and return stable empty arrays for no-space dashboards. |
| T-05-01-SC | Tampering | npm installs | low | accept | No package install or upgrade task exists; existing audited dependencies are reused. |
</threat_model>

<verification>
Plan-level checks:
- `npm test -- src/db/dashboard.query.test.ts`
- `npx tsc --noEmit`

No schema-relevant files are modified by this plan, so the schema push/migration gate is intentionally not triggered.
</verification>

<success_criteria>
The dashboard aggregate facade exists, all Phase 5 requirement IDs have query coverage, stored frozen USD fields are authoritative, bucket sums reconcile exactly, and no live FX recomputation path is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-overview/05-01-SUMMARY.md` when done.
</output>
