---
phase: 04-child-accounts-cascade-delete
plan: "05"
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - src/lib/money.ts
  - src/lib/money.test.ts
  - src/app/spaces/[id]/page.tsx
  - src/app/spaces/page.tsx
  - src/components/spaces/space-table.tsx
autonomous: true
requirements:
  - SPACE-05
must_haves:
  truths:
    - "Space displays keep frozen USD as the authoritative stored value, per D-15."
    - "Current CNY reference is computed from the cached CNY X-to-USD rate and is not stored, per D-16."
    - "When the CNY rate is unavailable, USD display remains and CNY shows a graceful fallback, per D-17."
  artifacts:
    - path: "src/lib/money.ts"
      provides: "USD minor to display-currency minor conversion helper"
    - path: "src/lib/money.test.ts"
      provides: "conversion direction and rounding tests"
    - path: "src/app/spaces/[id]/page.tsx"
      provides: "detail-page CNY reference display"
    - path: "src/components/spaces/space-table.tsx"
      provides: "space list CNY reference display"
  key_links:
    - from: "src/app/spaces/page.tsx"
      to: "src/db/fxRates.ts"
      via: "getRate(db, 'CNY')"
      pattern: "getRate\\(db, [\"']CNY[\"']\\)"
    - from: "src/components/spaces/space-table.tsx"
      to: "src/lib/money.ts"
      via: "helper converts frozen USD minor into current CNY minor"
      pattern: "usd.*Cny|convertUsd"
---

<objective>
Add display-only current CNY references and run the final Phase 04 verification gate.

Purpose: Users should see a CNY reference without changing frozen USD history or adding new stored CNY snapshot columns.
Output: Tested conversion helper, list/detail CNY reference displays, fallback state, and final automated verification for Phase 04.
</objective>

## Phase Goal

**As a** team subscription manager, **I want to** manage codex/chatgpt child accounts under each space and safely delete a space with all attached accounts, **so that** the account hierarchy stays accurate without orphaned records.

<execution_context>
@C:/Users/30952/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/30952/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-child-accounts-cascade-delete/04-CONTEXT.md
@.planning/phases/04-child-accounts-cascade-delete/04-RESEARCH.md
@.planning/phases/04-child-accounts-cascade-delete/04-PATTERNS.md
@.planning/phases/04-child-accounts-cascade-delete/04-UI-SPEC.md
@.planning/phases/04-child-accounts-cascade-delete/04-04-SUMMARY.md
@src/lib/money.ts
@src/lib/money.test.ts
@src/db/fxRates.ts
@src/app/spaces/[id]/page.tsx
@src/app/spaces/page.tsx
@src/components/spaces/space-table.tsx
</context>

## Artifacts this phase produces

- USD-to-current-CNY conversion helper in `src/lib/money.ts`
- CNY conversion tests in `src/lib/money.test.ts`
- Current CNY reference display on `src/app/spaces/[id]/page.tsx`
- Current CNY reference display on `src/components/spaces/space-table.tsx`
- `getRate(db, "CNY")` wiring in `src/app/spaces/page.tsx`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tested USD-to-CNY display conversion helper</name>
  <files>src/lib/money.ts, src/lib/money.test.ts</files>
  <read_first>
    @src/lib/money.ts
    @src/lib/money.test.ts
    @src/db/fxRates.ts
    @.planning/phases/04-child-accounts-cascade-delete/04-RESEARCH.md
  </read_first>
  <behavior>
    - The helper converts frozen USD minor units into a target currency minor-unit display value using the target currency's X-to-USD decimal string.
    - For CNY, conversion divides USD by the CNY-to-USD rate; it does not multiply USD by that rate.
    - Invalid, missing, zero, or negative rates are rejected by the helper or handled by the caller before display.
  </behavior>
  <action>Add a money helper with a name that clearly expresses USD minor to target-currency minor conversion, using BigInt decimal parsing and round-half-up behavior consistent with `freezeUsdMinor`. The helper must accept USD minor amount, target currency exponent, and the target currency's cached X-to-USD rate string. Extend `src/lib/money.test.ts` with cases proving the CNY direction from D-16, rounding behavior, and invalid-rate rejection. This implements D-15 and D-16 without adding any stored CNY columns.</action>
  <acceptance_criteria>
    - Tests fail if USD is multiplied by `CNY.rate_to_usd` instead of divided by it.
    - The helper uses integer/BigInt arithmetic and no floating point conversion.
    - Existing `freezeUsdMinor`, `parseToMinor`, and `formatMinor` tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/money.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>CNY reference math is tested and safe to use from RSC display code.</done>
</task>

<task type="auto">
  <name>Task 2: Show display-only CNY reference on space list and detail</name>
  <files>src/app/spaces/[id]/page.tsx, src/app/spaces/page.tsx, src/components/spaces/space-table.tsx</files>
  <read_first>
    @src/app/spaces/[id]/page.tsx
    @src/app/spaces/page.tsx
    @src/components/spaces/space-table.tsx
    @src/db/fxRates.ts
    @src/db/currencies.ts
    @src/lib/money.ts
    @src/lib/currencies.ts
    @.planning/phases/04-child-accounts-cascade-delete/04-UI-SPEC.md
  </read_first>
  <action>Read the current cached `CNY` rate in the spaces list RSC and detail RSC, derive current CNY reference values from each space's frozen `amountUsd`, and pass only display data into client components. Show frozen USD first and in primary foreground, then show the CNY reference as muted/secondary display text labeled by the UI spec. If `space.amountUsd`, the CNY currency metadata, or the CNY rate is unavailable, keep the USD display and show the D-17 fallback. Do not add `amount_cny`, `rate_cny`, `cny_snapshot`, or any other CNY persistence fields. This implements D-15, D-16, and D-17.</action>
  <acceptance_criteria>
    - Space list and detail both retain the existing frozen USD display.
    - Current CNY is visually secondary and labeled as a reference.
    - Missing CNY rate does not break rendering and shows the fallback.
    - `src/db/schema.ts` remains free of CNY snapshot columns.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>powershell -NoProfile -Command "`$schema = Get-Content -Raw 'src/db/schema.ts'; if (`$schema -match '(?i)cny.*(amount|rate|snapshot)|amount_cny|rate_cny') { throw 'CNY persistence column detected' }; `$table = Get-Content -Raw 'src/components/spaces/space-table.tsx'; if (`$table -notmatch 'CNY') { throw 'space table missing CNY reference display' }; `$detail = Get-Content -Raw -LiteralPath 'src/app/spaces/[id]/page.tsx'; if (`$detail -notmatch 'CNY') { throw 'detail page missing CNY reference display' }"</automated>
  </verify>
  <done>Space list and detail show CNY as a current display reference without mutating stored historical USD data.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 04 automated verification</name>
  <files>package.json, vitest.config.ts, tsconfig.json</files>
  <read_first>
    @package.json
    @vitest.config.ts
    @tsconfig.json
    @.planning/phases/04-child-accounts-cascade-delete/04-CONTEXT.md
    @.planning/phases/04-child-accounts-cascade-delete/04-RESEARCH.md
  </read_first>
  <action>Run the final automated gate after all schema, Server Action, and UI changes are complete. The mandatory schema generation gate already ran in Plan 04-01; this task verifies the integrated Phase 04 result. Do not skip failing tests. If a command fails because of an implementation issue, fix the implementation and rerun the targeted command before rerunning the full gate.</action>
  <acceptance_criteria>
    - DB cascade/no-orphan tests pass.
    - Child account action validation, no-rate, snapshot preservation, and refreeze tests pass.
    - Money conversion direction tests pass.
    - Type check, lint, and production build pass.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Phase 04 passes the full automated verification suite after all plans complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FX cache to display code | Cached CNY rate controls a derived display value. |
| RSC data to client table | Server-derived CNY reference values cross into client rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-TAMPER | Tampering | CNY conversion helper | medium | mitigate | Test the X-to-USD inversion direction and use integer/BigInt arithmetic. |
| T-04-05-INFO | Information Disclosure | CNY display | low | accept | Display uses existing cost fields and adds no new sensitive storage. |
| T-04-05-DOS | Denial of Service | missing CNY rate | medium | mitigate | Render fallback text while preserving USD display. |
| T-04-05-SC | Tampering | npm installs | low | accept | No package install or upgrade is required. |
</threat_model>

<verification>
Plan-level checks:
- `npx vitest run src/lib/money.test.ts`
- `npm test`
- `npx tsc --noEmit`
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
Frozen USD remains authoritative, current CNY appears only as display reference, missing CNY rates degrade gracefully, and the whole phase passes automated verification.
</success_criteria>

<output>
Create `.planning/phases/04-child-accounts-cascade-delete/04-05-SUMMARY.md` when done.
</output>
