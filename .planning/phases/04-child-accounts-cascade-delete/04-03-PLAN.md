---
phase: 04-child-accounts-cascade-delete
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/components/spaces/child-account-table.tsx
  - src/components/spaces/child-account-form.tsx
  - src/components/spaces/child-account-delete-dialog.tsx
  - src/components/spaces/mother-seat-card.tsx
  - src/app/spaces/[id]/page.tsx
autonomous: true
requirements:
  - ACCT-02
  - ACCT-03
must_haves:
  truths:
    - "Users manage child accounts only on `/spaces/[id]`, per D-01."
    - "Child accounts render in a table with add/edit dialogs and row delete confirmation, per D-02."
    - "Mother seat metadata is editable from the space detail page, per D-05."
  artifacts:
    - path: "src/components/spaces/child-account-table.tsx"
      provides: "detail-page child account table and row actions"
    - path: "src/components/spaces/child-account-form.tsx"
      provides: "child add/edit dialog form"
    - path: "src/components/spaces/child-account-delete-dialog.tsx"
      provides: "single-child delete confirmation"
    - path: "src/components/spaces/mother-seat-card.tsx"
      provides: "mother seat metadata display/edit control"
    - path: "src/app/spaces/[id]/page.tsx"
      provides: "space detail page integration"
  key_links:
    - from: "src/app/spaces/[id]/page.tsx"
      to: "src/db/childAccounts.ts"
      via: "listChildAccounts for detail page rows"
      pattern: "listChildAccounts\\(db, numericId\\)"
    - from: "src/components/spaces/child-account-form.tsx"
      to: "src/actions/childAccounts.ts"
      via: "create/update Server Actions"
      pattern: "createChildAccount|updateChildAccount"
---

<objective>
Deliver the detail-page child account and mother-seat management UI.

Purpose: Users must be able to perform ACCT-02 and ACCT-03 workflows without leaving the space detail page or exposing credential fields.
Output: Child account table, child add/edit/delete dialogs, mother-seat editor, and detail-page wiring for D-01 through D-11.
</objective>

## Phase Goal

**As a** team subscription manager, **I want to** manage codex/chatgpt child accounts under each space and safely delete a space with all attached accounts, **so that** the account hierarchy stays accurate without orphaned records.

<execution_context>
@C:/Users/30952/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/30952/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-child-accounts-cascade-delete/04-CONTEXT.md
@.planning/phases/04-child-accounts-cascade-delete/04-RESEARCH.md
@.planning/phases/04-child-accounts-cascade-delete/04-PATTERNS.md
@.planning/phases/04-child-accounts-cascade-delete/04-UI-SPEC.md
@.planning/phases/04-child-accounts-cascade-delete/04-02-SUMMARY.md
@src/app/spaces/[id]/page.tsx
@src/components/spaces/space-table.tsx
@src/components/spaces/space-form.tsx
@src/components/spaces/space-detail-actions.tsx
@src/components/channels/archive-dialog.tsx
@src/components/ui/dialog.tsx
@src/components/ui/alert-dialog.tsx
</context>

## Artifacts this phase produces

- `src/components/spaces/child-account-table.tsx`
- `ChildAccountTable`
- `src/components/spaces/child-account-form.tsx`
- `ChildAccountForm`
- `ChildAccountFormValue`
- `src/components/spaces/child-account-delete-dialog.tsx`
- `ChildAccountDeleteDialog`
- `src/components/spaces/mother-seat-card.tsx`
- `MotherSeatCard`
- Detail-page child account section in `src/app/spaces/[id]/page.tsx`

<tasks>

<task type="auto">
  <name>Task 1: Build child-account table, form, and delete dialog</name>
  <files>src/components/spaces/child-account-table.tsx, src/components/spaces/child-account-form.tsx, src/components/spaces/child-account-delete-dialog.tsx</files>
  <read_first>
    @.planning/phases/04-child-accounts-cascade-delete/04-UI-SPEC.md
    @src/components/spaces/space-table.tsx
    @src/components/spaces/space-form.tsx
    @src/components/channels/archive-dialog.tsx
    @src/actions/childAccounts.ts
    @src/lib/validation/childAccount.ts
    @src/lib/money.ts
    @src/lib/currencies.ts
  </read_first>
  <action>Create a client `ChildAccountTable` that accepts `spaceId`, child rows, and currencies, renders columns for seat type, email/login, note/label, joined date, monthly original price, frozen monthly USD, monthly payment day, and actions. Use `Plus`, `Pencil`, and `Trash2` lucide icons with tooltip-wrapped `size="icon"` buttons matching `SpaceTable`. Create `ChildAccountForm` using react-hook-form, `zodResolver(childAccountFormSchema)`, per-child currency select, amount parsing through `parseToMinor`, and Server Action calls to `createChildAccount` or `updateChildAccount`. Frozen USD fields must display as read-only table output, not editable inputs, per D-10. Create `ChildAccountDeleteDialog` with `AlertDialog` that deletes one child account only and explicitly says the space and mother account remain. Do not add credential fields or any global child route/sidebar, per D-01, D-02, D-06, D-07, D-08, D-09, D-10, and D-11.</action>
  <acceptance_criteria>
    - The child table has no password, token, API key, recovery code, cookie, or credential labels.
    - Add/edit uses dialogs and row edit/delete uses icon buttons with accessible labels.
    - Form submission parses money by selected currency minor unit and surfaces Server Action errors inline and through toast.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>powershell -NoProfile -Command "if (Test-Path 'src/app/child-accounts') { throw 'global child account route is not allowed' }; `$s = Get-Content -Raw 'src/components/spaces/child-account-form.tsx'; if (`$s -match '(?i)password|token|api key|recovery|cookie|credential') { throw 'credential-like child form copy detected' }"</automated>
  </verify>
  <done>Users can add, edit, and delete child account rows from a detail-page table without credential fields.</done>
</task>

<task type="auto">
  <name>Task 2: Wire child management and mother-seat editing into the detail page</name>
  <files>src/components/spaces/mother-seat-card.tsx, src/app/spaces/[id]/page.tsx</files>
  <read_first>
    @src/app/spaces/[id]/page.tsx
    @src/db/childAccounts.ts
    @src/actions/childAccounts.ts
    @src/components/spaces/space-detail-actions.tsx
    @src/components/spaces/child-account-table.tsx
    @.planning/phases/04-child-accounts-cascade-delete/04-UI-SPEC.md
  </read_first>
  <action>Create `MotherSeatCard` as a compact detail-page card/section that displays the mother account email, `seatType`, and `canChangeSeatType`, and opens an edit dialog using `motherSeatFormSchema` and `updateMotherSeat`. Update `src/app/spaces/[id]/page.tsx` to call `listChildAccounts(db, numericId)`, pass `currencies` and `spaceId` into `ChildAccountTable`, and replace the existing child-account placeholder card. Keep management inside `/spaces/[id]` only. This implements D-01, D-03, D-04, and D-05 while preserving D-02 table/dialog interaction.</action>
  <acceptance_criteria>
    - `/spaces/[id]` renders mother seat metadata and the child account section.
    - Child account empty state uses the UI-SPEC copy and primary add button.
    - Child accounts do not appear in sidebar/navigation or a global route.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>powershell -NoProfile -Command "`$p = Get-Content -Raw -LiteralPath 'src/app/spaces/[id]/page.tsx'; if (`$p -notmatch 'ChildAccountTable' -or `$p -notmatch 'MotherSeatCard' -or `$p -notmatch 'listChildAccounts') { throw 'detail page missing child or mother seat integration' }"</automated>
  </verify>
  <done>The space detail page exposes the complete ACCT-02 and ACCT-03 user workflow in the required location.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form to Server Action | UI collects untrusted child account and mother seat payloads. |
| Detail route params to DB reads | `/spaces/[id]` route param controls the queried space and child list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-TAMPER | Tampering | child account form | medium | mitigate | Client validation is convenience only; Server Actions from Plan 04-02 re-parse and validate. |
| T-04-03-INFO | Information Disclosure | child account UI | high | mitigate | Exclude credential fields from forms and source-check for credential-like labels. |
| T-04-03-SPOOF | Spoofing | route param `id` | low | mitigate | Preserve numeric route validation and `notFound()` behavior in `page.tsx`. |
| T-04-03-SC | Tampering | npm installs | low | accept | UI uses existing shadcn/lucide/sonner packages only. |
</threat_model>

<verification>
Plan-level checks:
- `npx tsc --noEmit`
- Source assertions for no global child route and no credential fields in child form.
</verification>

<success_criteria>
The user can manage child accounts and mother seat metadata from the space detail page only, using the existing table/dialog interaction style.
</success_criteria>

<output>
Create `.planning/phases/04-child-accounts-cascade-delete/04-03-SUMMARY.md` when done.
</output>
