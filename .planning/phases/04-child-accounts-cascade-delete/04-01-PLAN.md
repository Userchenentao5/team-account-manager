---
phase: 04-child-accounts-cascade-delete
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/0004_*.sql
  - drizzle/meta/_journal.json
  - src/db/childAccounts.ts
  - src/db/childAccounts.query.test.ts
autonomous: true
requirements:
  - ACCT-02
  - ACCT-03
  - SPACE-05
must_haves:
  truths:
    - "A space can own many child account rows with no credential columns, per D-01, D-06, and D-08."
    - "Mother accounts store editable seat metadata, per D-03, D-04, and D-05."
    - "Deleting a space at the database level cascades child_account and mother_account rows with no orphans, per D-14."
  artifacts:
    - path: "src/db/schema.ts"
      provides: "mother_account seat metadata and child_account table"
    - path: "drizzle/0004_*.sql"
      provides: "generated Drizzle migration evidence"
    - path: "src/db/childAccounts.ts"
      provides: "explicit-db child account and mother seat data helpers"
    - path: "src/db/childAccounts.query.test.ts"
      provides: "DB cascade and child CRUD regression tests"
  key_links:
    - from: "src/db/schema.ts"
      to: "drizzle/0004_*.sql"
      via: "npx drizzle-kit generate after schema edits"
      pattern: "child_account.*ON DELETE cascade"
    - from: "src/db/childAccounts.ts"
      to: "src/db/schema.ts"
      via: "imports childAccount and motherAccount table symbols"
      pattern: "childAccount|motherAccount"
---

<objective>
Create the persistent child-account and mother-seat foundation that supports Phase 04 user workflows.

Purpose: Users need child accounts under each space and mother accounts treated as seats, while destructive space deletion must leave no orphan rows.
Output: Schema, generated migration, explicit DB helpers, and migration-backed DB tests for ACCT-02, ACCT-03, SPACE-05, and D-03 through D-14.
</objective>

## Phase Goal

**As a** team subscription manager, **I want to** manage codex/chatgpt child accounts under each space and safely delete a space with all attached accounts, **so that** the account hierarchy stays accurate without orphaned records.

<execution_context>
@C:/Users/30952/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/30952/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-child-accounts-cascade-delete/04-CONTEXT.md
@.planning/phases/04-child-accounts-cascade-delete/04-RESEARCH.md
@.planning/phases/04-child-accounts-cascade-delete/04-PATTERNS.md
@src/db/schema.ts
@src/db/spaces.ts
@src/db/spaces.query.test.ts
@src/test/db-harness.ts
@drizzle.config.ts
</context>

## Artifacts this phase produces

- `childAccount` table export in `src/db/schema.ts`
- `ChildAccountRow` type export in `src/db/schema.ts`
- `motherAccount.seatType` and `motherAccount.canChangeSeatType` columns in `src/db/schema.ts`
- `drizzle/0004_*.sql` migration containing `child_account`, `mother_account.seat_type`, and `mother_account.can_change_seat_type`
- `src/db/childAccounts.ts`
- `listChildAccounts(db, spaceId)`
- `getChildAccount(db, id)`
- `insertChildAccount(db, values)`
- `updateChildAccount(db, id, values)`
- `deleteChildAccount(db, id)`
- `updateMotherSeat(db, spaceId, values)`
- `src/db/childAccounts.query.test.ts`

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | phase-04 | Manage child accounts and cascade-delete a space with mother and child accounts | 04-01, 04-02, 04-03, 04-04 | COVERED | Persistence, actions, UI, and destructive flow are planned.
REQ | ACCT-02 | Add child accounts under a space with type and email/login | 04-01, 04-02, 04-03 | COVERED | Schema, actions, and detail-page UI.
REQ | ACCT-03 | Edit and delete child accounts | 04-01, 04-02, 04-03 | COVERED | DB helpers, actions, row actions, delete dialog.
REQ | SPACE-05 | Delete space and cascade mother/child accounts | 04-01, 04-04 | COVERED | FK cascade plus transactional typed-name delete.
RESEARCH | schema | child_account table with FK cascade and mother seat defaults | 04-01 | COVERED | Uses Drizzle schema and generated migration.
RESEARCH | fx-snapshot | child monthly USD snapshot with no-rate block and preservation | 04-02 | COVERED | Server action tests cover save/update behavior.
RESEARCH | ui | detail-page table/dialog patterns with no global child route | 04-03 | COVERED | UI stays under /spaces/[id].
RESEARCH | cny | display-only current CNY reference, inverted rate direction | 04-05 | COVERED | Helper and UI display planned.
CONTEXT | D-01 | Detail-page-only child account management | 04-03 | COVERED | No global route/sidebar.
CONTEXT | D-02 | Table with row actions; dialog add/edit/delete | 04-03 | COVERED | Uses existing shadcn table/dialog patterns.
CONTEXT | D-03 | Mother and child accounts have seat_type | 04-01, 04-02, 04-03 | COVERED | Schema, action, UI.
CONTEXT | D-04 | can_change_seat_type only on mother_account | 04-01, 04-02, 04-03 | COVERED | Child schema excludes it.
CONTEXT | D-05 | Mother seat metadata editable from detail page | 04-02, 04-03 | COVERED | Action and component planned.
CONTEXT | D-06 | Child account field set | 04-01, 04-02, 04-03 | COVERED | Schema/action/form fields.
CONTEXT | D-07 | Per-child monthly currency | 04-01, 04-02, 04-03 | COVERED | monthly_currency_code field and form select.
CONTEXT | D-08 | No credential fields | 04-01, 04-02, 04-03 | COVERED | Schema and validation whitelist.
CONTEXT | D-09 | Monthly price uses integer minor units plus currency | 04-01, 04-02, 04-03 | COVERED | monthly_amount_minor and monthly_currency_code.
CONTEXT | D-10 | Freeze child monthly USD snapshot on save | 04-01, 04-02 | COVERED | Snapshot columns and action logic.
CONTEXT | D-11 | Block no-rate save | 04-02 | COVERED | Action validation tests.
CONTEXT | D-12 | Exact-name space delete confirmation | 04-04 | COVERED | UI and server validation.
CONTEXT | D-13 | Delete copy states space, mother, and children | 04-04 | COVERED | AlertDialog copy requirement.
CONTEXT | D-14 | One-transaction cascade with no orphans | 04-01, 04-04 | COVERED | FK cascade and transaction tests.
CONTEXT | D-15 | Frozen USD remains authoritative with CNY reference | 04-05 | COVERED | Display hierarchy planned.
CONTEXT | D-16 | CNY display-only from current cache; no frozen CNY columns | 04-05 | COVERED | Helper/display only.
CONTEXT | D-17 | Graceful CNY fallback | 04-05 | COVERED | Missing-rate fallback planned.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add child-account and mother-seat schema contracts</name>
  <files>src/db/schema.ts</files>
  <read_first>
    @.planning/phases/04-child-accounts-cascade-delete/04-CONTEXT.md
    @.planning/phases/04-child-accounts-cascade-delete/04-RESEARCH.md
    @.planning/phases/04-child-accounts-cascade-delete/04-PATTERNS.md
    @src/db/schema.ts
  </read_first>
  <behavior>
    - Existing mother_account rows migrate with non-null `seat_type` and `can_change_seat_type` defaults.
    - New child_account rows belong to exactly one space and cascade when that space is deleted.
    - Child account storage includes D-06 billing/snapshot fields and excludes credential-like fields per D-08.
  </behavior>
  <action>Add `seatType` and `canChangeSeatType` to `motherAccount` with non-null defaults that let existing Phase 03 rows migrate: `seatType` defaults to `codex` and `canChangeSeatType` defaults to `true`, per the resolved mother-account default decision in `04-RESEARCH.md`. Add `childAccount` via `sqliteTable("child_account", ...)` with `spaceId` referencing `space.id` using `{ onDelete: "cascade" }`; `seatType` constrained at the application layer to `codex` or `chatgpt`; `email`, `label`, `joinedDate`, `monthlyAmountMinor`, `monthlyCurrencyCode`, `monthlyRateUsed`, `monthlyRateAsOf`, `monthlyRateSource`, `monthlyAmountUsd`, `monthlyPaymentDay`, and timestamp fields matching local style. Add exported row/insert/update types. Do not add password, token, API key, recovery code, cookie, credential, or CNY snapshot columns. This implements D-03, D-04, D-06, D-07, D-08, D-09, D-10, and supports D-14.</action>
  <acceptance_criteria>
    - `src/db/schema.ts` exports `childAccount` and `ChildAccountRow`.
    - `motherAccount` contains only the two new seat metadata fields plus existing identity fields.
    - `childAccount.spaceId` has `onDelete: "cascade"`.
    - No credential-like or CNY snapshot columns are present in `childAccount`.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Schema compiles and exposes the exact table/column contracts needed by downstream DB, action, and UI tasks.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate and inspect Drizzle migration evidence</name>
  <files>drizzle/0004_*.sql, drizzle/meta/_journal.json</files>
  <read_first>
    @drizzle.config.ts
    @src/db/schema.ts
    @.planning/phases/04-child-accounts-cascade-delete/04-PATTERNS.md
  </read_first>
  <action>Run `npx drizzle-kit generate` after Task 1 schema edits. Inspect the generated `drizzle/0004_*.sql` and metadata to confirm it adds `mother_account.seat_type` defaulting to `codex`, `mother_account.can_change_seat_type` defaulting to `true`, and `child_account` with `space_id` cascade behavior, per the resolved mother-account default decision in `04-RESEARCH.md`. Do not run `npx drizzle-kit push` unless local live DB state must be updated for implementation; if `push` prompts interactively in a non-TTY session, stop and create a blocking manual intervention note instead of bypassing the prompt. This task is the mandatory schema push / migration verification gate before any action or UI verification.</action>
  <acceptance_criteria>
    - Exactly one new `drizzle/0004_*.sql` migration exists for Phase 04.
    - The migration can be consumed by the existing in-memory migration harness.
    - The migration includes child account FK cascade, `mother_account.seat_type` default `codex`, and `mother_account.can_change_seat_type` default `true`.
  </acceptance_criteria>
  <verify>
    <automated>npx drizzle-kit generate</automated>
    <automated>powershell -NoProfile -Command "`$m = Get-ChildItem drizzle -Filter '0004_*.sql' | Select-Object -First 1; if (-not `$m) { throw 'missing 0004 migration' }; `$s = Get-Content -Raw `$m.FullName; if (`$s -notmatch 'child_account' -or `$s -notmatch 'space_id' -or `$s -notmatch 'ON DELETE cascade' -or `$s -notmatch 'seat_type' -or `$s -notmatch 'can_change_seat_type') { throw 'migration missing required Phase 04 schema evidence' }"</automated>
  </verify>
  <done>Generated migration evidence exists before later tests rely on the migrated schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add child-account DB helpers and cascade tests</name>
  <files>src/db/childAccounts.ts, src/db/childAccounts.query.test.ts</files>
  <read_first>
    @src/db/schema.ts
    @src/db/spaces.ts
    @src/db/spaces.query.test.ts
    @src/test/db-harness.ts
    @.planning/phases/04-child-accounts-cascade-delete/04-PATTERNS.md
  </read_first>
  <behavior>
    - `listChildAccounts(db, spaceId)` returns child rows for one space only, sorted deterministically.
    - Child create/update/delete helpers mutate only the requested child rows.
    - Deleting a parent space through SQLite FK behavior removes child rows and mother rows.
    - `updateMotherSeat(db, spaceId, values)` updates only `seatType` and `canChangeSeatType`.
  </behavior>
  <action>Create `src/db/childAccounts.ts` using the explicit-db pattern from `src/db/spaces.ts`. Implement `listChildAccounts`, `getChildAccount`, `insertChildAccount`, `updateChildAccount`, `deleteChildAccount`, and `updateMotherSeat`. Return typed Drizzle rows and keep helpers free of the `@/db` singleton. Create `src/db/childAccounts.query.test.ts` using `createTestDb()` and `seedCurrencies()`. Tests must cover insert/list/update/delete, per-space isolation, mother seat update fields from D-03 through D-05, child absence of `can_change_seat_type` per D-04, and no orphan child or mother rows when the parent `space` row is deleted per D-14.</action>
  <acceptance_criteria>
    - DB helpers accept an explicit `db` argument and use Drizzle builders.
    - Tests prove child rows are attached to a space and removed by FK cascade.
    - Tests prove mother seat metadata can be updated without adding child-level `can_change_seat_type`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/db/childAccounts.query.test.ts src/db/spaces.query.test.ts</automated>
  </verify>
  <done>ACCT-02, ACCT-03, and the database side of SPACE-05 are migration-backed and covered by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drizzle schema to SQLite | Schema declarations become the real persistence boundary for cascade and credential exclusion. |
| Test harness to migration files | In-memory tests prove generated migrations match schema expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-TAMPER | Tampering | `child_account.space_id` FK | high | mitigate | Use `references(() => space.id, { onDelete: "cascade" })` and migration/test assertions for no orphan rows. |
| T-04-01-INFO | Information Disclosure | `child_account` schema | high | mitigate | Whitelist D-06 fields only and assert no credential-like columns are added. |
| T-04-01-INTEGRITY | Tampering | `mother_account` defaults | medium | mitigate | Add non-null defaults so existing rows migrate consistently and remain editable later. |
| T-04-01-SC | Tampering | npm installs | low | accept | No new packages are installed in this plan; existing Drizzle tooling is used. |
</threat_model>

<verification>
Plan-level checks:
- `npx drizzle-kit generate`
- `npx vitest run src/db/childAccounts.query.test.ts src/db/spaces.query.test.ts`
- `npx tsc --noEmit`
</verification>

<success_criteria>
Child account persistence exists, generated migration evidence is present, and tests prove child/mother rows do not survive parent space deletion.
</success_criteria>

<output>
Create `.planning/phases/04-child-accounts-cascade-delete/04-01-SUMMARY.md` when done.
</output>
