---
phase: 01-foundations-schema-reference-data
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/spaces/page.tsx
  - src/app/reference-data/currencies/page.tsx
  - src/components/nav/sidebar.tsx
autonomous: false
requirements: [REF-02]
must_haves:
  truths:
    - "User sees a persistent left sidebar with 仪表盘 / 空间 / 参考数据 (→ 支付渠道 / 币种) nav items"
    - "The active route is highlighted with the accent (exactly one active item)"
    - "User can open 参考数据 → 币种 and see a read-only table of all 6 seeded currencies with code / name / minor-unit"
    - "Dashboard (/) and Spaces (/spaces) render placeholder pages"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout rendering the sidebar nav shell (D-12)"
      contains: "Sidebar"
    - path: "src/components/nav/sidebar.tsx"
      provides: "Left-sidebar nav with the three top-level groups + lucide icons + active highlighting"
    - path: "src/app/reference-data/currencies/page.tsx"
      provides: "RSC currency list — the one real DB read of the walking skeleton"
      contains: "currency"
  key_links:
    - from: "src/app/reference-data/currencies/page.tsx"
      to: "src/db/schema.ts"
      via: "RSC selects from the currency table via the db singleton"
      pattern: "db\\.select|from\\(currency"
    - from: "src/app/layout.tsx"
      to: "src/components/nav/sidebar.tsx"
      via: "root layout mounts the sidebar shell"
      pattern: "Sidebar|sidebar"
---

<objective>
Deliver the navigation shell (D-12) and the read slice of the walking skeleton: a left-sidebar nav (仪表盘 / 空间 / 参考数据 → 支付渠道 / 币种) and a server-rendered, read-only currency list that reads the 6 seeded currencies LIVE from SQLite. This is the "one real DB read" + the working app shell.

Purpose: Satisfies ROADMAP Success Criterion 1 (working navigation shell) and Success Criterion 3 (view supported currencies for later space selection) and implements REF-02's display side.
Output: A navigable app where the user clicks 币种 and sees the seeded currencies; Dashboard/Spaces are placeholders.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/CLAUDE.md
@.planning/phases/01-foundations-schema-reference-data/01-UI-SPEC.md
@.planning/phases/01-foundations-schema-reference-data/01-CONTEXT.md
@.planning/phases/01-foundations-schema-reference-data/SKELETON.md
@src/db/schema.ts
@src/db/index.ts
</context>

<artifacts_this_phase_produces>
- Route `/` → Dashboard placeholder (`src/app/page.tsx`).
- Route `/spaces` → Spaces placeholder (`src/app/spaces/page.tsx`).
- Route `/reference-data/currencies` → read-only currency list RSC (`src/app/reference-data/currencies/page.tsx`).
- `src/components/nav/sidebar.tsx` → the left-sidebar nav shell component (consumed by `src/app/layout.tsx`).
- Updated `src/app/layout.tsx` → root layout mounting the sidebar around `{children}`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build the left-sidebar navigation shell (D-12)</name>
  <files>src/components/nav/sidebar.tsx, src/app/layout.tsx, src/app/page.tsx, src/app/spaces/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundations-schema-reference-data/01-UI-SPEC.md (Navigation shell labels; Interaction & State Contract — sidebar; Color — accent reserved for active item)
    - .planning/phases/01-foundations-schema-reference-data/01-CONTEXT.md (D-12)
    - src/app/layout.tsx (scaffold default from Plan 01 — to be replaced)
  </read_first>
  <action>
    Create `src/components/nav/sidebar.tsx` using the shadcn `sidebar` primitive (already installed in Plan 01). Render three top-level groups with lucide icons and Simplified-Chinese labels per UI-SPEC: 仪表盘 (LayoutDashboard → `/`), 空间 (Boxes → `/spaces`), 参考数据 (with children 支付渠道 CreditCard → `/reference-data/channels`, 币种 Coins → `/reference-data/currencies`). Highlight the active route with the accent (background tint + icon), exactly one active item at a time, using `usePathname()` in a client component. Icon-only/collapse controls carry `aria-label`.

    Update `src/app/layout.tsx` to mount the sidebar shell around `{children}` (light mode, Geist Sans body, sonner Toaster at the root for later plans). Create the placeholder pages `src/app/page.tsx` (仪表盘 placeholder card: "仪表盘将在后续阶段实现") and `src/app/spaces/page.tsx` (空间 placeholder card). The 支付渠道 nav link points at `/reference-data/channels` (route delivered by Plan 03) — it is a plain href, no compile dependency.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/nav/sidebar.tsx` contains the labels 仪表盘, 空间, 参考数据, 支付渠道, 币种 and links `/`, `/spaces`, `/reference-data/channels`, `/reference-data/currencies`.
    - `src/components/nav/sidebar.tsx` uses `usePathname` for active-state highlighting.
    - `src/app/layout.tsx` mounts the sidebar component and a sonner `Toaster`.
    - `src/app/page.tsx` and `src/app/spaces/page.tsx` render placeholder content (not the create-next-app default).
    - `npx next build` succeeds.
  </acceptance_criteria>
  <done>Persistent left sidebar renders with the three groups, lucide icons, and active-route highlighting; Dashboard and Spaces are reachable placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Render the read-only currency list (REF-02, the real DB read)</name>
  <files>src/app/reference-data/currencies/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundations-schema-reference-data/01-UI-SPEC.md (Currency list copy: columns 代码/名称/最小单位位数 + read-only caption)
    - .planning/phases/01-foundations-schema-reference-data/01-CONTEXT.md (D-01..D-04)
    - src/db/schema.ts, src/db/index.ts (currency table + db singleton)
  </read_first>
  <action>
    Create `src/app/reference-data/currencies/page.tsx` as a Server Component (RSC) that imports the `db` singleton and selects all rows from the `currency` table directly (no client fetch layer — Pattern 1). Render a read-only shadcn `table` with columns 代码 (code, Geist Mono), 名称 (name), 最小单位位数 (minor_unit). Add the read-only caption from UI-SPEC: `币种列表用于创建空间时选择,暂不支持在此编辑。` (D-04 — display only, no edit UI). No actions, no dialogs. Page title 币种 at the Display size.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/reference-data/currencies/page.tsx` selects from the `currency` table via the `db` singleton (`db.select` ... `currency`).
    - The rendered table has the three columns 代码 / 名称 / 最小单位位数 and the read-only caption text.
    - No `'use client'` directive and no Server Action/dialog in this file (read-only RSC).
    - `npx next build` succeeds with the page included.
  </acceptance_criteria>
  <done>Visiting /reference-data/currencies server-renders the 6 seeded currencies live from SQLite with the read-only caption.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — navigation shell + currency list</name>
  <action>Automation is complete; pause for the user to verify the running app per the steps below. Run `npm run dev` so the user can exercise the nav shell and the live currency list, then wait for the resume signal.</action>
  <what-built>The navigation shell (left sidebar: 仪表盘 / 空间 / 参考数据 → 支付渠道 / 币种 with active highlighting) and the read-only currency list rendered live from SQLite.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000.
    2. Confirm the left sidebar shows 仪表盘, 空间, 参考数据 (expandable to 支付渠道 and 币种), each with an icon.
    3. Click 仪表盘 and 空间 — placeholder pages render; the clicked item is highlighted (exactly one active item).
    4. Click 参考数据 → 币种. Confirm a read-only table lists 6 rows: USD, CNY, EUR, GBP, JPY, HKD, with the 最小单位位数 column showing 0 for JPY and 2 for the rest, plus the read-only caption.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., missing nav item, wrong currency rows, no active highlight).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC | read-only navigation; no user input crosses into the DB in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-READ | Information disclosure | currency RSC page | low | accept | Currency list is non-sensitive reference data (no credentials/PII); single-user local tool, no auth by design (CLAUDE.md) |
| T-02-EDGE | Denial of service (crash) | currencies/page.tsx DB read | low | mitigate | RSC runs on Node runtime (no `runtime = 'edge'`); reuses the Node-only `db` singleton (Pitfall 2) |
</threat_model>

<verification>
- `npx next build` succeeds.
- Sidebar renders with all three groups and active highlighting.
- /reference-data/currencies shows 6 currency rows read live from SQLite.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
- ROADMAP Success Criterion 1 (working navigation shell) met.
- ROADMAP Success Criterion 3 (viewable currency list) met.
- REF-02 display side complete; the walking-skeleton "one real DB read" demonstrated end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-schema-reference-data/01-02-SUMMARY.md` when done.
</output>
