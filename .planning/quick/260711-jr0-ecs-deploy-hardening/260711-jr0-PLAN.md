---
quick_task: 260711-jr0
description: Harden the existing GitHub Actions deployment without changing the single-ECS architecture
---

<objective>
Fix confirmed CI/CD correctness and security gaps while preserving the current Docker Compose deployment model.
</objective>

<tasks>
<task type="auto">
<name>Task 1: Harden the GitHub Actions deployment workflow</name>
<files>.github/workflows/deploy.yml</files>
<action>Add PR validation, production deploy serialization, fail-fast Bash over SSH, safe environment-file permissions, SSH connection timeouts, delayed rsync updates, compose validation, an external health probe, and deployed revision recording. Remove the redundant remote cleanup routine.</action>
<verify>GitHub Actions workflow runs lint, tests, build, Docker deployment, and the external health probe successfully.</verify>
<done>Only successful main pushes deploy, and deployment failures return a non-zero status instead of being masked by later remote commands.</done>
</task>

<task type="auto">
<name>Task 2: Make runtime health explicit and align Node versions</name>
<files>src/app/api/health/route.ts, middleware.ts, compose.yaml, Dockerfile, package.json</files>
<action>Add an unauthenticated health route used by Compose and deployment checks. Move CI and Docker to Node 24.18.0 and declare the supported Node engine.</action>
<verify>npm run lint; npm test; npm run build</verify>
<done>The service health check no longer relies on a login redirect, and CI and the container use the same supported Node release.</done>
</task>
</tasks>
